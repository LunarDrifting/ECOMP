---
phase: 01.5-approval-immutable-decisions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_approval_model/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: true
requirements:
  - PH1.5-APPROVAL-MODEL
  - PH1.5-TENANT-SCOPED-APPROVAL
  - PH1.5-IMMUTABLE-DECISION-SEMANTICS
  - PH1.5-INVERSE-RELATIONS
  - PH1.5-SCHEMA-ONLY-DISCIPLINE
user_setup: []
must_haves:
  truths:
    - "Prisma schema defines Approval model with required fields: taskId, actorId, tenantId, decision, comment, createdAt."
    - "Approval.decision enum is explicitly limited to APPROVED and REJECTED."
    - "Approval references Task, User, and Tenant via required foreign keys."
    - "Approval includes @@index([tenantId])."
    - "Task and User include required inverse relations to Approval."
    - "Approval is modeled with immutable decision semantics (append-only records; no updatedAt/update-oriented field)."
    - "No existing model fields are modified, renamed, or removed except required inverse relation additions."
    - "A Prisma migration is generated and applied without direct DB edits."
    - "No service or route files are modified."
  artifacts:
    - "prisma/schema.prisma includes ApprovalDecision enum and Approval model plus required inverse relation additions."
    - "prisma/migrations/*_add_approval_model/migration.sql creates Approval structures, constraints, and foreign keys."
  key_links:
    - "Approval.taskId references Task.id."
    - "Approval.actorId references User.id."
    - "Approval.tenantId references Tenant.id."
    - "Task has inverse relation field to Approval."
    - "User has inverse relation field to Approval."
---

<objective>
Introduce an Approval domain model that records immutable approval/rejection decisions for tasks while preserving strict architecture constraints.

Purpose: establish append-only approval decision data semantics without adding runtime/business logic.
Output: updated `prisma/schema.prisma` and a Prisma migration for Approval structures only.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/01.5-approval-immutable-decisions/01.5-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Approval schema with immutable decision semantics</name>
  <files>prisma/schema.prisma</files>
  <action>Add `ApprovalDecision` enum with exact values `APPROVED` and `REJECTED`. Add `Approval` model with required fields: taskId, actorId, tenantId, decision, comment nullable, createdAt. Add required relations to Task, User, and Tenant. Add `@@index([tenantId])`. Add only required inverse relation fields on Task and User. Preserve immutability semantics in schema by not adding `updatedAt` to Approval and by modeling decisions as append-only records. Do not modify existing model fields other than required inverse relation additions.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema validates and includes Approval model, enum values, required relations, inverse relations, and immutable semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for Approval schema changes</name>
  <files>prisma/migrations/*_add_approval_model/migration.sql, prisma/migrations/migration_lock.toml</files>
  <action>Run Prisma migration creation (`npx prisma migrate dev --name add_approval_model`). Confirm generated SQL creates Approval enum/table, required foreign keys to Task/User/Tenant, and tenant index. Ensure migration scope is additive and does not modify existing model fields except required inverse relation support.</action>
  <verify>
    <automated>npx prisma migrate dev --name add_approval_model</automated>
  </verify>
  <done>Migration file is created/applied and SQL reflects required Approval schema and foreign-key links.</done>
</task>

<task type="auto">
  <name>Task 3: Run generation and baseline checks with immutability review</name>
  <files>prisma/schema.prisma</files>
  <action>Inspect schema/migration diff to verify only additive Approval changes plus required inverse relation additions were made, and that Approval has no update-oriented schema fields. Run Prisma client generation and project lint/build checks to confirm no runtime breakage from schema-only changes. Report pre-existing baseline issues without refactoring.</action>
  <verify>
    <automated>git diff -- prisma/schema.prisma prisma/migrations | cat && npx prisma generate && npm run lint && npm run build</automated>
  </verify>
  <done>Diff confirms immutability and scope constraints, Prisma client is generated, and baseline checks complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate dev --name add_approval_model` succeeds
- [ ] Approval includes `taskId`, `actorId`, `tenantId`, `decision`, `comment`, `createdAt`
- [ ] `ApprovalDecision` enum includes exactly `APPROVED`, `REJECTED`
- [ ] Approval references `Task`, `User`, and `Tenant`
- [ ] Approval includes `@@index([tenantId])`
- [ ] Task and User include inverse relation to Approval
- [ ] Approval has immutable decision semantics (no `updatedAt`)
- [ ] Existing model fields unchanged except required inverse relation additions
- [ ] `npx prisma generate` succeeds
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced by Approval schema changes
- Approval immutable-decision model is present and migrated
- Phase remains schema-only with sequential execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
