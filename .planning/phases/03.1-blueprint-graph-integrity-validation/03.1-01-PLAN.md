---
phase: 03.1-blueprint-graph-integrity-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/app/api/ecos/[id]/instantiate/route.ts
autonomous: true
requirements:
  - PH3.1-BLUEPRINT-PREVALIDATION
  - PH3.1-SELF-EDGE-REJECTION
  - PH3.1-CYCLE-DETECTION
  - PH3.1-NO-PARTIAL-WRITES-ON-FAIL
  - PH3.1-DETERMINISTIC-ERROR-MAPPING
  - PH3.1-TENANT-ISOLATION
  - PH3.1-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "instantiateTemplateForEco runs blueprint graph validation before any ECOPlan/task/dependency/state writes."
    - "TemplateTaskDefinition and TemplateDependencyDefinition rows are batch-loaded once per instantiation under tenant scope."
    - "Blueprint self-edges (fromDefinitionId === toDefinitionId) are rejected deterministically."
    - "Blueprint cycles are detected with deterministic in-memory algorithm (DFS or Kahn) without N+1 queries."
    - "Invalid blueprint returns deterministic 409 behavior with error message: Invalid blueprint: circular dependency detected."
    - "On invalid blueprint, no ECOPlan/task/runtime dependency writes occur."
    - "Valid blueprint path preserves existing instantiation flow semantics unchanged."
    - "Idempotent no-op behavior remains preserved."
    - "Approval and gate enforcement logic is unchanged."
    - "Projection logic is unchanged."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "src/services/template-instantiation.service.ts includes pre-instantiation blueprint validation block and deterministic cycle detection."
    - "src/lib/db.ts exposes any required batched tenant-scoped read helpers used by validation."
    - "src/app/api/ecos/[id]/instantiate/route.ts maps invalid blueprint validation errors to deterministic 409 response."
  key_links:
    - "Validation executes before ECOPlan ensure/create step."
    - "Cycle/self-edge failures short-circuit all downstream write operations."
    - "Route preserves thin-service pattern and deterministic error mapping."
---

<objective>
Enforce blueprint dependency graph integrity before runtime materialization.

Purpose: prevent runtime writes from invalid blueprint dependency graphs by validating self-edges and cycles ahead of instantiation.
Output: pre-instantiation validation in service, batched tenantDb reads, and deterministic route error mapping.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/03.1-blueprint-graph-integrity-validation/03.1-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/ecos/[id]/instantiate/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure batched tenantDb reads support blueprint graph validation</name>
  <files>src/lib/db.ts</files>
  <action>Confirm/add tenant-scoped batched read helpers needed for validation inputs: template task definitions and template dependency definitions by templateVersionId + tenantId. Ensure no per-node query behavior is introduced and all reads remain wrapper-scoped.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb provides required batched reads for validation without N+1 query patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Add pre-instantiation blueprint self-edge + cycle validation in service</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Inside instantiateTemplateForEco, before ECOPlan creation and all write operations, load blueprint definitions/dependencies and run in-memory deterministic graph validation. Reject on self-edge and cycle detection with deterministic validation error. Validation must fail fast and avoid per-node queries. Ensure invalid blueprint path exits before ECOPlan/task/dependency/state writes, and valid blueprint path continues existing flow unchanged.</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>Service performs pre-write blueprint integrity validation and blocks invalid graphs deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Keep instantiate endpoint deterministic on blueprint validation failures</name>
  <files>src/app/api/ecos/[id]/instantiate/route.ts</files>
  <action>Update route error mapping to return deterministic 409 with required invalid-blueprint message for self-edge/cycle failures. Keep route thin and Prisma-free. Verify that no partial creations occur on validation failures and that valid DAG instantiation still succeeds.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/ecos/REPLACE_ECO_ID/instantiate -H 'content-type: application/json' -d '{"tenantId":"REPLACE_TENANT_ID","templateVersionId":"REPLACE_TEMPLATE_VERSION_ID"}' || true)</automated>
  </verify>
  <done>Route maps invalid-blueprint errors deterministically and preserves existing service delegation pattern.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Blueprint validation executes before ECOPlan/task/dependency/state writes
- [ ] TemplateTaskDefinition rows loaded in one batched read
- [ ] TemplateDependencyDefinition rows loaded in one batched read
- [ ] Self-edge blueprint dependencies are rejected
- [ ] Simple cycle (A->B->A) rejected deterministically
- [ ] Longer cycle (A->B->C->A) rejected deterministically
- [ ] Invalid blueprint returns deterministic 409 with required error payload/message
- [ ] No ECOPlan/task/runtime dependency writes occur on invalid blueprint rejection
- [ ] Valid DAG blueprint still instantiates successfully
- [ ] Idempotent no-op behavior remains unchanged
- [ ] Approval/gate enforcement logic remains unchanged
- [ ] Projection logic remains unchanged
- [ ] No schema/model changes
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Invalid blueprint graphs are blocked before any runtime writes
- Valid DAG blueprints continue current instantiation behavior
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
