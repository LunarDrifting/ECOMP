---
phase: 03.1-blueprint-graph-integrity-validation
plan: "01"
subsystem: template-instantiation
tags: [service-layer, validation, blueprint, dependency-graph, tenant-isolation]
requires: []
provides:
  - Pre-instantiation blueprint graph integrity validation
  - Deterministic invalid blueprint error mapping on instantiate route
affects: [workflow-write-side, api]
tech-stack:
  added: []
  patterns: [batched reads, in-memory cycle detection, deterministic validation errors]
key-files:
  created: []
  modified:
    - src/services/template-instantiation.service.ts
    - src/app/api/ecos/[id]/instantiate/route.ts
key-decisions:
  - "Validation now executes before ECOPlan/task/dependency/state writes in instantiateTemplateForEco."
  - "Invalid blueprint graph uses a deterministic message and maps to HTTP 409 in the existing instantiate route."
  - "Cycle detection is in-memory DFS over batched blueprint reads; no per-node query patterns introduced."
patterns-established:
  - "Service remains Prisma-free via tenantDb wrapper and enforces tenant-scoped batched reads."
  - "Route remains thin and delegates instantiation logic to service."
requirements-completed:
  - PH3.1-BLUEPRINT-PREVALIDATION
  - PH3.1-SELF-EDGE-REJECTION
  - PH3.1-CYCLE-DETECTION
  - PH3.1-NO-PARTIAL-WRITES-ON-FAIL
  - PH3.1-DETERMINISTIC-ERROR-MAPPING
  - PH3.1-TENANT-ISOLATION
  - PH3.1-SCHEMA-STABILITY
duration: 24min
completed: 2026-02-28
---

# Phase 3.1 Plan 01 Summary

**Added pre-write blueprint dependency graph validation to instantiation and deterministic 409 mapping for invalid blueprints.**

## Performance

- **Duration:** 24 min
- **Started:** 2026-02-28T10:35:00Z
- **Completed:** 2026-02-28T10:59:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Confirmed `tenantDb` already had required batched tenant-scoped read helpers for:
  - `TemplateTaskDefinition` by `templateVersionId + tenantId`
  - `TemplateDependencyDefinition` by `templateVersionId + tenantId`
- Added service-level blueprint graph validation in `instantiateTemplateForEco` before all write operations:
  - loads blueprint definitions and dependencies once each (batched)
  - rejects self-edges (`fromDefinitionId === toDefinitionId`)
  - performs deterministic in-memory DFS cycle detection and rejects cyclic graphs
  - returns deterministic validation error message: `Invalid blueprint: circular dependency detected`
- Updated instantiate route error mapping to return deterministic `409` for invalid blueprint graph validation failures.

## Task Commits

Each implementation task was committed atomically:

1. **Task 2: Pre-instantiation blueprint graph validation in service** - `5f02929` (feat)
2. **Task 3: Deterministic route mapping for invalid blueprint errors** - `5d125a0` (feat)

Task 1 required no code changes because the needed batched `tenantDb` helpers were already present.

## Files Created/Modified

- `src/services/template-instantiation.service.ts` - Added blueprint graph validation helper and invoked validation before ECOPlan/task/dependency/state writes.
- `src/app/api/ecos/[id]/instantiate/route.ts` - Added deterministic 409 mapping for invalid blueprint graph error.

## Decisions Made

- Used a single canonical validation error message for both self-edge and cycle failures to keep API behavior deterministic.
- Kept missing-definition mapping checks in existing flow unchanged; this phase only enforced self-edge/cycle integrity pre-write.

## Deviations from Plan

- `src/lib/db.ts` was not modified since required helpers already existed and met constraints.

## Issues Encountered

- `npm run lint` still reports pre-existing baseline issues in unchanged files (`src/lib/db.ts` `no-explicit-any`, warning in `src/app/api/admin-only/route.ts`).
- `curl` verification against `localhost:3000` failed because no local server was running.

## Verification Evidence

- `npx eslint src/services/template-instantiation.service.ts 'src/app/api/ecos/[id]/instantiate/route.ts` passed.
- `npm run lint` failed only on pre-existing baseline issues outside this phase scope.
- `npm run build` passed (run with escalated permissions due sandbox process restrictions).
- `curl -i --max-time 5 -X POST http://localhost:3000/api/ecos/REPLACE_ECO_ID/instantiate ...` failed with connection error (no server on port 3000).
- `prisma/schema.prisma` unchanged.

## User Setup Required

None.

## Next Phase Readiness

- Instantiation now deterministically blocks invalid blueprint graphs before runtime writes.
- Valid blueprint DAG flows continue through existing task/dependency/state materialization behavior.

---
*Phase: 03.1-blueprint-graph-integrity-validation*
*Completed: 2026-02-28*
