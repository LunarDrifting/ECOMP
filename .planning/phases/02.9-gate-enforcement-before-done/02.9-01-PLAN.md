---
phase: 02.9-gate-enforcement-before-done
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/app/api/tasks/[id]/complete/route.ts
autonomous: true
requirements:
  - PH2.9-PRECONDITION-LOAD
  - PH2.9-MINIMAL-GATE-EVALUATION
  - PH2.9-GATE-FAIL-REJECTION
  - PH2.9-RESOLVER-GUARDED-CALL
  - PH2.9-IDEMPOTENCY-PRESERVATION
  - PH2.9-STRUCTURED-RESULT
  - PH2.9-TENANT-ISOLATION
  - PH2.9-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "markTaskDone enforces PRECONDITION gates after approval enforcement and before DONE mutation."
    - "markTaskDone loads tenant-scoped Gate rows for task filtered to PRECONDITION."
    - "Minimal evaluator uses condition.allow === true as pass criterion; all other values fail."
    - "If any PRECONDITION gate fails, service rejects deterministically with 409 behavior."
    - "Gate rejection performs no state mutation and does not call resolveDependenciesForTask."
    - "If all PRECONDITION gates pass, DONE update + resolver call proceed."
    - "Existing BLOCKED rejection, approval enforcement, and already-DONE idempotency are preserved."
    - "Result includes taskMarkedDone, tasksUnblocked, unblockedTaskIds, approvalCheckPassed, gateCheckPassed."
    - "POSTCONDITION behavior is not implemented in this phase."
    - "No background workers are introduced."
    - "Route remains thin and Prisma-free."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "src/lib/db.ts includes tenant-scoped PRECONDITION gate lookup helper(s)."
    - "src/services/template-instantiation.service.ts enforces PRECONDITION gates in markTaskDone and returns gateCheckPassed."
    - "src/app/api/tasks/[id]/complete/route.ts maps deterministic gate failures to 409."
  key_links:
    - "markTaskDone control flow: blocked check -> already-done check -> approval check -> precondition gate check -> DONE mutation -> resolver."
    - "Resolver is called only on successful completion path."
    - "Gate evaluation is intentionally minimal and scoped to PRECONDITION only."
---

<objective>
Enforce PRECONDITION gates in markTaskDone before allowing DONE transitions.

Purpose: prevent DONE transitions when required preconditions fail while preserving approval/dependency safeguards and idempotency.
Output: service/db wrapper updates and endpoint integration with gate-aware completion result.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.9-gate-enforcement-before-done/02.9-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/tasks/[id]/complete/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper for PRECONDITION gate lookup</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped helper(s) to load Gate rows for taskId filtered by type PRECONDITION, exposing condition JSON needed by markTaskDone. Keep wrapper-only data access and strict tenant scoping.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes tenant-safe PRECONDITION gate retrieval for completion checks.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce PRECONDITION gates in markTaskDone</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Update markTaskDone to run PRECONDITION checks after approval enforcement and before DONE mutation. Evaluate each gate condition with minimal evaluator (`condition.allow === true` passes; otherwise fails). If any gate fails: deterministic conflict rejection, no state mutation, no resolver call. If all pass: continue existing DONE update + resolver flow. Preserve BLOCKED rejection, approval enforcement, and already-DONE idempotency. Add `gateCheckPassed` to structured result alongside existing fields.</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>markTaskDone enforces PRECONDITION gates with deterministic behavior and gateCheckPassed result output.</done>
</task>

<task type="auto">
  <name>Task 3: Keep completion endpoint thin with deterministic gate rejection mapping</name>
  <files>src/app/api/tasks/[id]/complete/route.ts</files>
  <action>Update route error mapping for gate failures to deterministic 409 while keeping route Prisma-free and thin. Ensure success payload surfaces gateCheckPassed. Run lint/build/curl checks and confirm no schema/model changes.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/tasks/REPLACE_TASK_ID/complete -H 'content-type: application/json' -d '{"tenantId":"REPLACE_TENANT_ID"}' || true)</automated>
  </verify>
  <done>Completion route remains thin and deterministically reports gate enforcement outcomes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] markTaskDone loads tenant-scoped PRECONDITION gates for the task
- [ ] Minimal evaluator applied: condition.allow === true passes, otherwise fails
- [ ] Any PRECONDITION failure returns deterministic 409 behavior
- [ ] Gate failure path performs no state mutation
- [ ] Gate failure path does not call resolveDependenciesForTask
- [ ] Success path still performs DONE update and resolver call
- [ ] Existing BLOCKED rejection, approval enforcement, and already-DONE idempotency preserved
- [ ] Result includes taskMarkedDone, tasksUnblocked, unblockedTaskIds, approvalCheckPassed, gateCheckPassed
- [ ] POSTCONDITION behavior not implemented
- [ ] Route remains thin and Prisma-free
- [ ] `prisma/schema.prisma` unchanged
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- PRECONDITION gate failures prevent DONE transitions deterministically
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
