---
phase: 01.4-ecoplan-template-binding
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_ecoplan_model/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: true
requirements:
  - PH1.4-ECOPLAN-MODEL
  - PH1.4-ECO-INVERSE-RELATION
  - PH1.4-EXPLICIT-CONSTRAINTS
  - PH1.4-NON-CASCADE-TEMPLATEVERSION
  - PH1.4-SCHEMA-ONLY-DISCIPLINE
user_setup: []
must_haves:
  truths:
    - "Prisma schema defines ECOPlan with ecoId, templateVersionId, tenantId, createdAt, and updatedAt."
    - "ECOPlan enforces a one-to-one binding from ECO to ECOPlan via @@unique([ecoId])."
    - "ECOPlan includes @@index([tenantId]) and @@index([templateVersionId])."
    - "ECOPlan references TemplateVersion and Tenant via foreign keys."
    - "TemplateVersion to ECOPlan relation does not use cascade delete."
    - "ECO includes inverse relation to ECOPlan."
    - "No existing model fields are modified, renamed, or removed except required inverse relation additions."
    - "A Prisma migration is generated and applied without direct DB edits."
    - "No business logic code is added or changed."
  artifacts:
    - "prisma/schema.prisma includes model ECOPlan and required inverse relation on ECO."
    - "prisma/migrations/*_add_ecoplan_model/migration.sql creates ECOPlan table, constraints, and foreign keys."
  key_links:
    - "ECOPlan.ecoId references ECO.id with @@unique([ecoId]) to enforce 1:1."
    - "ECOPlan.templateVersionId references TemplateVersion.id."
    - "ECOPlan.tenantId references Tenant.id."
    - "ECO has inverse relation field to ECOPlan."
---

<objective>
Introduce the ECOPlan domain model that binds ECO instances to template versions while preserving strict tenant architecture and schema-only constraints.

Purpose: establish the data-model contract for ECO-to-template binding with no runtime/business-logic changes.
Output: updated `prisma/schema.prisma` and a Prisma migration for ECOPlan structures only.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/01.4-ecoplan-template-binding/01.4-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ECOPlan model and required inverse relation in schema</name>
  <files>prisma/schema.prisma</files>
  <action>Add `ECOPlan` model with required fields: ecoId, templateVersionId, tenantId, createdAt, updatedAt. Add relations from ECOPlan to ECO, TemplateVersion, and Tenant. Enforce ECO 1:1 binding explicitly with `@@unique([ecoId])`. Add explicit indexes `@@index([tenantId])` and `@@index([templateVersionId])`. Ensure relation from TemplateVersion to ECOPlan does not use cascade delete (use `Restrict` or default behavior). Add inverse relation on ECO to ECOPlan as required. Do not alter existing model fields other than that required inverse relation addition. Do not rename existing fields. Keep changes schema-only.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema validates and includes ECOPlan with required fields/relations plus ECO inverse relation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for ECOPlan schema changes</name>
  <files>prisma/migrations/*_add_ecoplan_model/migration.sql, prisma/migrations/migration_lock.toml</files>
  <action>Run Prisma migration creation (`npx prisma migrate dev --name add_ecoplan_model`). Confirm generated SQL adds ECOPlan table, required FKs, explicit unique/index constraints (`@@unique([ecoId])`, index on tenantId, index on templateVersionId), and non-cascade behavior for TemplateVersion relation. Ensure migration scope is additive and does not modify existing model fields except required inverse relation support.</action>
  <verify>
    <automated>npx prisma migrate dev --name add_ecoplan_model</automated>
  </verify>
  <done>Migration file is created/applied and SQL reflects required ECOPlan structure with 1:1 ECO uniqueness.</done>
</task>

<task type="auto">
  <name>Task 3: Run generation and baseline checks with immutability review</name>
  <files>prisma/schema.prisma</files>
  <action>Inspect schema/migration diff to verify no existing fields were modified/renamed except required inverse relation addition. Run Prisma client generation and project lint/build checks to confirm no runtime breakage from schema-only changes. Report any pre-existing baseline issues without refactoring.</action>
  <verify>
    <automated>git diff -- prisma/schema.prisma prisma/migrations | cat && npx prisma generate && npm run lint && npm run build</automated>
  </verify>
  <done>Diff confirms constraint compliance, Prisma client is generated, and baseline checks complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate dev --name add_ecoplan_model` succeeds
- [ ] ECOPlan includes `ecoId`, `templateVersionId`, `tenantId`, `createdAt`, `updatedAt`
- [ ] ECOPlan references `ECO`, `TemplateVersion`, and `Tenant`
- [ ] ECO 1:1 binding enforced via `@@unique([ecoId])`
- [ ] ECOPlan includes `@@index([tenantId])`
- [ ] ECOPlan includes `@@index([templateVersionId])`
- [ ] TemplateVersion relation does not cascade-delete ECOPlan records
- [ ] ECO has inverse relation to ECOPlan
- [ ] Existing model fields unchanged except required inverse relation addition
- [ ] `npx prisma generate` succeeds
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced by ECOPlan schema changes
- ECOPlan binding model is present and migrated
- Phase remains schema-only with sequential execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
