---
phase: 02.1-template-task-definition-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_template_task_definition_model/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: true
requirements:
  - PH2.1-TEMPLATE-TASK-DEFINITION-MODEL
  - PH2.1-TENANT-SCOPED-DEFINITION
  - PH2.1-OWNER-ROLE-RELATION
  - PH2.1-ENUM-REUSE
  - PH2.1-SELF-RELATION
  - PH2.1-TEMPLATEVERSION-INVERSE
  - PH2.1-SCHEMA-ONLY-DISCIPLINE
user_setup: []
must_haves:
  truths:
    - "Prisma schema defines TemplateTaskDefinition with required fields, including tenantId."
    - "TemplateTaskDefinition reuses existing enums: TaskLevel, TaskVisibility, ApprovalPolicy, ClockMode."
    - "TemplateTaskDefinition references Tenant and includes @@index([tenantId])."
    - "TemplateTaskDefinition.ownerRoleId explicitly references Role.id with onDelete: Restrict and includes @@index([ownerRoleId])."
    - "TemplateTaskDefinition references TemplateVersion and TemplateVersion has required inverse relation."
    - "TemplateTaskDefinition includes nullable self-parent relation with children inverse."
    - "No existing model fields are modified, renamed, or removed beyond required inverse relation additions."
    - "A Prisma migration is generated and applied without direct DB edits."
  artifacts:
    - "prisma/schema.prisma includes TemplateTaskDefinition model and TemplateVersion inverse relation."
    - "prisma/migrations/*_add_template_task_definition_model/migration.sql creates table, constraints, and foreign keys."
  key_links:
    - "TemplateTaskDefinition.templateVersionId references TemplateVersion.id."
    - "TemplateTaskDefinition.tenantId references Tenant.id."
    - "TemplateTaskDefinition.ownerRoleId references Role.id with onDelete: Restrict."
    - "TemplateVersion includes inverse relation to TemplateTaskDefinition."
    - "TemplateTaskDefinition.parentDefinitionId references TemplateTaskDefinition.id (nullable)."
---

<objective>
Introduce TemplateTaskDefinition schema entities to support future blueprint-driven workflow instantiation.

Purpose: establish hierarchical template task definition data structures without runtime/business-logic changes.
Output: updated `prisma/schema.prisma` and migration for TemplateTaskDefinition only.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.1-template-task-definition-model/02.1-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TemplateTaskDefinition model with enum reuse and self relation</name>
  <files>prisma/schema.prisma</files>
  <action>Add `TemplateTaskDefinition` with required fields: templateVersionId, tenantId, parentDefinitionId nullable, name, taskLevel(TaskLevel), ownerRoleId, visibility(TaskVisibility), approvalPolicy(ApprovalPolicy), clockMode(ClockMode), createdAt. Add required relations to TemplateVersion and Tenant, plus self-referential parent/children relation. Define ownerRoleId relation to Role with `onDelete: Restrict`. Add required inverse relation field on TemplateVersion. Add required indexes `@@index([tenantId])` and `@@index([ownerRoleId])`. Do not modify existing model fields beyond required inverse relation additions; only additive changes allowed.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema validates with required model fields, enum reuse, TemplateVersion inverse relation, and self hierarchy relation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for TemplateTaskDefinition changes</name>
  <files>prisma/migrations/*_add_template_task_definition_model/migration.sql, prisma/migrations/migration_lock.toml</files>
  <action>Run Prisma migration creation (`npx prisma migrate dev --name add_template_task_definition_model`). Confirm generated SQL creates TemplateTaskDefinition table and required foreign keys to TemplateVersion, Tenant, Role (ownerRoleId with RESTRICT semantics), and self-reference parentDefinitionId, plus required indexes on tenantId and ownerRoleId.</action>
  <verify>
    <automated>npx prisma migrate dev --name add_template_task_definition_model</automated>
  </verify>
  <done>Migration file is created/applied and SQL reflects required structure and relations.</done>
</task>

<task type="auto">
  <name>Task 3: Run generation and baseline quality checks</name>
  <files>prisma/schema.prisma</files>
  <action>Inspect schema/migration diff to verify only additive TemplateTaskDefinition changes and inverse relation additions were made. Run Prisma generate plus lint/build checks. Report pre-existing lint baseline issues without unrelated refactoring.</action>
  <verify>
    <automated>git diff -- prisma/schema.prisma prisma/migrations | cat && npx prisma generate && npm run lint && npm run build</automated>
  </verify>
  <done>Diff confirms scope discipline and build tooling checks complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate dev --name add_template_task_definition_model` succeeds
- [ ] TemplateTaskDefinition includes all required fields
- [ ] TemplateTaskDefinition includes required `tenantId` relation to Tenant
- [ ] Existing enums are reused (no duplicate enum definitions)
- [ ] ownerRoleId relation targets Role with `onDelete: Restrict`
- [ ] `@@index([tenantId])` and `@@index([ownerRoleId])` exist
- [ ] TemplateVersion inverse relation exists
- [ ] Self parent/children relation exists and parentDefinitionId is nullable
- [ ] Existing model fields unchanged beyond required inverse relation additions
- [ ] `npx prisma generate` succeeds
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced by schema changes
- TemplateTaskDefinition model and migration are present
- Phase remains schema-only with sequential execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
