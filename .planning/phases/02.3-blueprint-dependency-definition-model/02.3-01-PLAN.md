---
phase: 02.3-blueprint-dependency-definition-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_template_dependency_definition_model/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: true
requirements:
  - PH2.3-TEMPLATE-DEPENDENCY-DEFINITION-MODEL
  - PH2.3-TENANT-SCOPED-DEFINITION
  - PH2.3-TEMPLATEVERSION-RELATION
  - PH2.3-TEMPLATE-TASK-DEFINITION-EDGE-RELATIONS
  - PH2.3-DEPENDENCYTYPE-ENUM-REUSE
  - PH2.3-INDEX-DISCIPLINE
  - PH2.3-SCHEMA-ONLY-DISCIPLINE
user_setup: []
must_haves:
  truths:
    - "Prisma schema defines TemplateDependencyDefinition with required fields: templateVersionId, tenantId, fromDefinitionId, toDefinitionId, type, lagMinutes, createdAt."
    - "TemplateDependencyDefinition reuses existing DependencyType enum (no new dependency-type enum introduced)."
    - "TemplateDependencyDefinition references TemplateVersion."
    - "TemplateDependencyDefinition references TemplateTaskDefinition for both fromDefinitionId and toDefinitionId with explicit relation names: TemplateDependencyFrom and TemplateDependencyTo."
    - "TemplateTaskDefinition includes additive inverse relations: outgoingDependencies @relation(\"TemplateDependencyFrom\") and incomingDependencies @relation(\"TemplateDependencyTo\")."
    - "TemplateDependencyDefinition includes @@index([tenantId]), @@index([fromDefinitionId]), and @@index([toDefinitionId])."
    - "Self-dependency (fromDefinitionId == toDefinitionId) is documented as disallowed at service layer, not enforced in this schema phase."
    - "No existing model fields are modified, renamed, or removed."
    - "A Prisma migration is generated and applied with no direct DB edits."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "prisma/schema.prisma includes TemplateDependencyDefinition and required additive inverse relations only."
    - "prisma/migrations/*_add_template_dependency_definition_model/migration.sql creates table, constraints, and indexes."
  key_links:
    - "TemplateDependencyDefinition.templateVersionId references TemplateVersion.id."
    - "TemplateDependencyDefinition.tenantId references Tenant.id."
    - "TemplateDependencyDefinition.fromDefinitionId references TemplateTaskDefinition.id via @relation(\"TemplateDependencyFrom\")."
    - "TemplateDependencyDefinition.toDefinitionId references TemplateTaskDefinition.id via @relation(\"TemplateDependencyTo\")."
    - "TemplateTaskDefinition.outgoingDependencies uses @relation(\"TemplateDependencyFrom\")."
    - "TemplateTaskDefinition.incomingDependencies uses @relation(\"TemplateDependencyTo\")."
---

<objective>
Introduce TemplateDependencyDefinition schema entities to support blueprint-driven dependency graph instantiation.

Purpose: establish tenant-scoped dependency edges between template task definitions without runtime/business-logic changes.
Output: updated `prisma/schema.prisma` and migration for TemplateDependencyDefinition only.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.3-blueprint-dependency-definition-model/02.3-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TemplateDependencyDefinition model and required relations</name>
  <files>prisma/schema.prisma</files>
  <action>Add `TemplateDependencyDefinition` with required fields: templateVersionId, tenantId, fromDefinitionId, toDefinitionId, type(DependencyType), lagMinutes(Int), createdAt. Add required relations to TemplateVersion and Tenant. Add required relations to TemplateTaskDefinition for both fromDefinitionId and toDefinitionId using explicit names exactly: `@relation("TemplateDependencyFrom")` and `@relation("TemplateDependencyTo")`. Add required inverse relations on TemplateTaskDefinition: `outgoingDependencies TemplateDependencyDefinition[] @relation("TemplateDependencyFrom")` and `incomingDependencies TemplateDependencyDefinition[] @relation("TemplateDependencyTo")`. Add required indexes: @@index([tenantId]), @@index([fromDefinitionId]), @@index([toDefinitionId]). Do not modify existing model fields; allow only additive relation fields where required for Prisma relation completeness.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema validates with TemplateDependencyDefinition, required references, and required indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for TemplateDependencyDefinition changes</name>
  <files>prisma/migrations/*_add_template_dependency_definition_model/migration.sql, prisma/migrations/migration_lock.toml</files>
  <action>Run Prisma migration creation (`npx prisma migrate dev --name add_template_dependency_definition_model`). Confirm generated SQL creates TemplateDependencyDefinition table with foreign keys to Tenant, TemplateVersion, and TemplateTaskDefinition (from/to), and indexes on tenantId, fromDefinitionId, and toDefinitionId.</action>
  <verify>
    <automated>npx prisma migrate dev --name add_template_dependency_definition_model</automated>
  </verify>
  <done>Migration file is created/applied and SQL reflects required table, constraints, and indexes.</done>
</task>

<task type="auto">
  <name>Task 3: Scope check and baseline quality checks</name>
  <files>prisma/schema.prisma, prisma/migrations/*</files>
  <action>Review schema and migration diff to ensure phase is schema-only and no existing model fields were modified. Run Prisma generate plus lint/build checks. Report pre-existing baseline issues without unrelated refactoring.</action>
  <verify>
    <automated>git diff -- prisma/schema.prisma prisma/migrations | cat && npx prisma generate && npm run lint && npm run build</automated>
  </verify>
  <done>Diff confirms scope discipline and build tooling checks complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate dev --name add_template_dependency_definition_model` succeeds
- [ ] TemplateDependencyDefinition includes all required fields
- [ ] Existing `DependencyType` enum is reused
- [ ] TemplateDependencyDefinition references TemplateVersion
- [ ] TemplateDependencyDefinition references TemplateTaskDefinition for both fromDefinitionId and toDefinitionId
- [ ] Relation names are explicitly declared as `TemplateDependencyFrom` and `TemplateDependencyTo`
- [ ] TemplateTaskDefinition includes `outgoingDependencies` and `incomingDependencies` inverse relations with matching explicit relation names
- [ ] `@@index([tenantId])`, `@@index([fromDefinitionId])`, and `@@index([toDefinitionId])` exist
- [ ] Self-dependency restriction is documented as service-layer validation only (not schema enforcement in this phase)
- [ ] No existing model fields are modified
- [ ] `npx prisma generate` succeeds
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced by schema changes
- TemplateDependencyDefinition model and migration are present
- Phase remains schema-only with sequential execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
