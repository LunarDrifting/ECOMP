---
phase: 03.6-workflow-engine-test-harness-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tests/helpers/workflow-fixtures.ts
  - tests/helpers/test-db.ts
  - tests/integration/workflow-engine.integration.test.ts
autonomous: true
requirements:
  - PH3.6-TEST-HARNESS-FOUNDATION
  - PH3.6-TEST-DB-ISOLATION
  - PH3.6-INSTANTIATION-BEHAVIOR-COVERAGE
  - PH3.6-COMPLETION-GUARD-COVERAGE
  - PH3.6-POLICY-GATE-COVERAGE
  - PH3.6-BLUEPRINT-VALIDATION-COVERAGE
  - PH3.6-NO-PRODUCTION-BEHAVIOR-CHANGE
  - PH3.6-NO-SCHEMA-CHANGES
  - PH3.6-DETERMINISTIC-TESTS
user_setup: []
must_haves:
  truths:
    - "Vitest is configured as the single integration test framework for this phase."
    - "A dedicated test DB connection is used via `DATABASE_URL_TEST` (or equivalent) separate from default DB."
    - "Test harness fails fast if `DATABASE_URL_TEST` is missing."
    - "Tests never use or fall back to `DATABASE_URL`."
    - "No production service/route behavior is modified by this phase."
    - "Instantiation integration test verifies blocked/ready counts sum to tasksCreated."
    - "Completing BLOCKED task deterministically rejects with 409 behavior."
    - "Completing NOT_STARTED task can unblock downstream tasks where dependencies are satisfied."
    - "Re-completing DONE task returns deterministic idempotent no-op response."
    - "Approval policy failure path returns canonical 409 and preserves state."
    - "Gate failure path returns 409 and preserves state."
    - "Cyclic blueprint validation rejects before writes with no ECOPlan/tasks/dependency persistence."
    - "Tests remain deterministic (no Date.now-based assertions)."
    - "No schema/model files are modified."
    - "Execution remains sequential."
  artifacts:
    - "Vitest config and npm test script are present and runnable."
    - "Test bootstrap guard enforces `DATABASE_URL_TEST` presence and blocks `DATABASE_URL` fallback."
    - "Reusable test fixtures/helpers provide deterministic tenant-scoped setup/teardown."
    - "At least 6 integration tests exist in workflow-engine integration suite covering required behaviors."
  key_links:
    - "Test data setup/teardown uses direct Prisma in test utilities, not dev seed route HTTP path."
    - "Integration tests target workflow engine behavior end-to-end through service/API execution path."
    - "Build remains passing after test harness introduction."
---

<objective>
Add a minimal deterministic integration test harness for workflow engine behavior.

Purpose:
Protect critical workflow engine guarantees with repeatable automated integration tests.

Output:
Vitest-based integration suite + helpers + scripts with required behavior coverage.
</objective>

<execution_context>
Use standard GSD execute workflow and summary template.
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/services/approval.service.ts
@src/services/state-transition.service.ts
@src/app/api/ecos/[id]/instantiate/route.ts
@src/app/api/tasks/[id]/complete/route.ts
@prisma/schema.prisma
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vitest harness and deterministic test DB plumbing</name>
  <files>package.json, vitest.config.ts, tests/helpers/test-db.ts</files>
  <action>
Install/configure Vitest and add `npm run test` script. Create deterministic test DB helper wiring that uses `DATABASE_URL_TEST` (or equivalent explicit test DB env), supports setup/cleanup, and avoids touching production DB data.
Require fail-fast behavior when test DB env is missing and explicitly prevent use of `DATABASE_URL`.
  </action>
  <verify>
    <automated>npm run test -- --runInBand || npm run test</automated>
  </verify>
  <done>Test runner is configured and executes against isolated test DB context.</done>
</task>

<task type="auto">
  <name>Task 2: Build workflow fixture utilities for tenant-scoped deterministic setup</name>
  <files>tests/helpers/workflow-fixtures.ts, tests/helpers/test-db.ts</files>
  <action>
Add helper utilities to seed tenant, roles, actor, template version, template task/dependency definitions, ECO, and optional approvals/gates directly via Prisma in test setup. Ensure deterministic naming and cleanup per test case.
  </action>
  <verify>
    <automated>npm run test -- --runInBand tests/integration/workflow-engine.integration.test.ts || npm run test</automated>
  </verify>
  <done>Fixtures support deterministic setup/teardown for all required scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Implement integration tests for core workflow engine behaviors</name>
  <files>tests/integration/workflow-engine.integration.test.ts</files>
  <action>
Create at least 6 integration tests covering required behavior set:
- instantiation counts
- blocked completion rejection
- successful completion unblocking path
- done idempotency
- approval policy failure no-mutation
- gate failure no-mutation
- cyclic blueprint pre-write rejection
Use deterministic assertions (no Date.now checks) and enforce tenant scoping assumptions.
For cyclic blueprint rejection, assert no partial writes by querying DB:
- no ECOPlan for ecoId
- no Task rows for ecoId
- no Dependency rows for any created task ids (or none created)
  </action>
  <verify>
    <automated>npm run test && npm run build</automated>
  </verify>
  <done>Integration suite verifies core workflow engine guarantees end-to-end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Vitest is configured and `npm run test` exists
- [ ] Tests require `DATABASE_URL_TEST` (or equivalent) and fail fast if missing
- [ ] Tests never use `DATABASE_URL`
- [ ] Tests run against isolated test DB env
- [ ] Instantiation test validates blocked+ready equals tasksCreated
- [ ] BLOCKED completion test rejects with 409 behavior
- [ ] NOT_STARTED completion test succeeds and unblocks downstream where applicable
- [ ] DONE idempotency test returns no-op semantics
- [ ] Approval policy failure test returns canonical 409 and confirms no mutation
- [ ] Gate failure test returns 409 and confirms no mutation
- [ ] Cyclic blueprint rejection test confirms no partial writes:
- [ ] no ECOPlan exists for ecoId
- [ ] no Task rows exist for ecoId
- [ ] no Dependency rows exist for created task ids (or none were created)
- [ ] No schema/model changes
- [ ] No production behavior changes
- [ ] Tests are deterministic
- [ ] `npm run build` passes
</verification>

<success_criteria>
- Deterministic integration harness exists and is runnable
- Core workflow engine behaviors are covered by automated tests
- Build remains healthy and production behavior remains unchanged
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
