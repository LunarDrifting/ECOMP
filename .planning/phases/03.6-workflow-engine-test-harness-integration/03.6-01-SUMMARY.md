---
phase: 03.6-workflow-engine-test-harness-integration
plan: "01"
subsystem: test-harness
tags: [integration-tests, vitest, workflow-engine, deterministic-tests]
requires: []
provides:
  - Vitest integration test harness scaffolding
  - deterministic test DB guardrails and fixtures
  - workflow engine integration test suite coverage
affects: [test-tooling]
tech-stack:
  added: [vitest (declared)]
  patterns: [fail-fast env checks, deterministic fixtures, route-level integration assertions]
key-files:
  created:
    - vitest.config.ts
    - tests/helpers/test-db.ts
    - tests/helpers/workflow-fixtures.ts
    - tests/integration/workflow-engine.integration.test.ts
  modified:
    - package.json
    - tsconfig.json
key-decisions:
  - "Selected Vitest as single framework and added npm test script (`vitest run`)."
  - "Test harness hard-requires DATABASE_URL_TEST and rejects DATABASE_URL mismatch to prevent accidental non-test DB usage."
  - "Cyclic blueprint coverage includes explicit no-partial-write DB assertions (no ECOPlan/tasks/dependencies for eco)."
  - "Excluded tests from app TypeScript build scope so production build remains deterministic when test-only deps are unavailable."
patterns-established:
  - "Direct Prisma setup/teardown helpers for deterministic, tenant-scoped integration fixtures."
  - "Route-level integration tests using NextRequest + app route handlers without background workers."
requirements-completed:
  - PH3.6-TEST-HARNESS-FOUNDATION
  - PH3.6-TEST-DB-ISOLATION
  - PH3.6-INSTANTIATION-BEHAVIOR-COVERAGE
  - PH3.6-COMPLETION-GUARD-COVERAGE
  - PH3.6-POLICY-GATE-COVERAGE
  - PH3.6-BLUEPRINT-VALIDATION-COVERAGE
  - PH3.6-NO-PRODUCTION-BEHAVIOR-CHANGE
  - PH3.6-NO-SCHEMA-CHANGES
  - PH3.6-DETERMINISTIC-TESTS
duration: 34min
completed: 2026-02-28
---

# Phase 3.6 Plan 01 Summary

**Added a deterministic Vitest-based workflow engine integration test harness, fixtures, and scenario coverage without schema or production behavior changes.**

## Performance

- **Duration:** 34 min
- **Started:** 2026-02-28T13:05:00Z
- **Completed:** 2026-02-28T13:39:00Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added Vitest harness scaffolding:
  - `vitest.config.ts`
  - `npm run test` script in `package.json`
- Added deterministic test DB guardrails:
  - `tests/helpers/test-db.ts` enforces `DATABASE_URL_TEST` presence
  - rejects mismatched `DATABASE_URL`
  - uses direct Prisma for setup/teardown and DB reset
  - runs `prisma migrate deploy` against test DB at test startup
- Added reusable deterministic workflow fixtures:
  - `tests/helpers/workflow-fixtures.ts`
  - tenant/roles/actors creation
  - blueprint and task fixture helpers
- Added integration suite with required scenarios:
  - `tests/integration/workflow-engine.integration.test.ts`
  - instantiation blocked+ready count consistency
  - blocked completion rejection (409)
  - successful completion unblocks downstream
  - DONE idempotency
  - approval policy failure canonical 409 + no mutation
  - gate failure 409 + no mutation
  - cyclic blueprint rejection + no partial writes (ECOPlan/tasks/dependencies)
- Kept production build stable by excluding tests from app TS build scope in `tsconfig.json`.

## Task Commits

1. **Task 1: Vitest harness + DB guardrails** - `11d090e` (test)
2. **Task 2: Deterministic fixture helpers** - `2e5fc08` (test)
3. **Task 3: Integration test suite coverage** - `a91740d` (test)

## Verification Evidence

- `npx eslint vitest.config.ts tests/helpers/test-db.ts tests/helpers/workflow-fixtures.ts tests/integration/workflow-engine.integration.test.ts` passed.
- `npm run build` passed (with escalated permissions in sandbox).
- `npm run test` currently fails in this environment because `vitest` binary is unavailable (`sh: vitest: command not found`).
- Attempted `npm install --save-dev vitest` failed due network/DNS restriction to npm registry (`ENOTFOUND registry.npmjs.org`).
- `prisma/schema.prisma` unchanged.

## Scope Guardrails Preserved

- No schema changes.
- No production runtime behavior changes.
- No background workers introduced.
- Tenant isolation assumptions preserved in fixtures and assertions.

## User Setup Required

- Install test dependencies when network access is available:
  - `npm install`
- Then run:
  - `npm run test`

## Next Phase Readiness

- Integration harness scaffolding and deterministic fixtures are in place; once `vitest` is installed, the suite can execute end-to-end against `DATABASE_URL_TEST`.

---
*Phase: 03.6-workflow-engine-test-harness-integration*
*Completed: 2026-02-28*
