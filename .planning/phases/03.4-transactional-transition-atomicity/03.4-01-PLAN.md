---
phase: 03.4-transactional-transition-atomicity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/services/approval.service.ts
autonomous: true
requirements:
  - PH3.4-ATOMIC-DONE-TRANSITION
  - PH3.4-ATOMIC-RESOLVER-CALL
  - PH3.4-ROLLBACK-ON-FAILURE
  - PH3.4-IDEMPOTENT-TRANSACTION-BOUNDARY
  - PH3.4-TENANT-ISOLATION
  - PH3.4-NO-SCHEMA-CHANGES
user_setup: []
must_haves:
  truths:
    - "markTaskDone executes DONE mutation + resolver unblocking inside a single transactional boundary."
    - "If any step in completion flow fails, no state mutation persists."
    - "Approval enforcement, PRECONDITION gate checks, and authz checks execute before transaction begins."
    - "Transaction scope includes DONE state mutation and all resolver-induced state updates."
    - "Resolver does not perform its own nested transaction."
    - "Resolver must reuse the transaction client provided by markTaskDone and must not open a nested transaction."
    - "Already-DONE idempotency remains deterministic and avoids unnecessary transaction writes."
    - "BLOCKED rejection still short-circuits before transaction."
    - "Projection logic remains unchanged."
    - "No schema/model files are modified."
    - "Execution remains sequential."
  artifacts:
    - "src/services/template-instantiation.service.ts wraps DONE + resolver logic in single prisma.$transaction block."
    - "tenantDb exposes transactional wrapper entrypoint if needed."
    - "Resolver reuses provided transactional context instead of standalone writes."
  key_links:
    - "Transaction boundary begins only after all pre-mutation validation succeeds."
    - "Transaction boundary includes: DONE update + downstream unblocking updates."
    - "No partial DONE-without-unblock or unblock-without-DONE states are possible."
---

<objective>
Enforce atomic state transitions for DONE completion and downstream dependency resolution.

Purpose: guarantee deterministic, rollback-safe transitions with no partial writes.

Output: transactional service-layer transition with no schema changes.
</objective>

<execution_context>
Use standard GSD execute workflow and summary template.
</execution_context>

<context>
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/services/approval.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce transactional wrapper support in tenantDb</name>
  <files>src/lib/db.ts</files>
  <action>
Add a safe wrapper that allows service-layer methods to run inside prisma.$transaction while preserving tenant scoping.
Explicitly require tenantDb to accept an optional Prisma transaction client (`tx`), binding all wrapper methods exclusively to either base `prisma` or provided `tx`.
Ensure no service-layer code references base prisma client while executing inside transaction scope.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>tenantDb supports transactional execution context.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap DONE transition and resolver in single transaction</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>
Refactor markTaskDone so that:
- All validation (BLOCKED, already-DONE, authz, approval policy, PRECONDITION gates) occurs before transaction.
- Use `prisma.$transaction(async (tx) => { ... })`.
- Instantiate tenantDb as `tenantDb(tenantId, tx)` inside the transaction.
- DONE mutation + resolveDependenciesForTask execution occur using the same `tx` client.
- resolveDependenciesForTask accepts a `tx` parameter and does not open its own transaction.
- No nested transactions introduced.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>Completion flow is atomic and rollback-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Validate rollback safety and deterministic behavior</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>
Ensure that:
- Any thrown error inside transaction aborts all writes.
- Resolver cannot partially unblock tasks if DONE mutation fails.
- Idempotent behavior preserved.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Atomic transition integrity guaranteed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] DONE + resolver updates occur in single transaction
- [ ] No partial writes possible
- [ ] Validation remains pre-transaction
- [ ] tenantDb supports tx-bound execution and no base prisma calls occur inside transaction scope
- [ ] Already-DONE idempotency preserved
- [ ] BLOCKED rejection preserved
- [ ] Approval/gate enforcement preserved
- [ ] No nested transactions
- [ ] No schema changes
- [ ] npm run build passes
</verification>

<success_criteria>
- Atomic DONE transition enforced
- Resolver updates rollback-safe
- Write-side state integrity guaranteed
</success_criteria>
