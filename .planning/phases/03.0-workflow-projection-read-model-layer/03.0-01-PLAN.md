---
phase: 03.0-workflow-projection-read-model-layer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/workflow-projection.service.ts
  - src/app/api/ecos/[id]/projection/route.ts
autonomous: true
requirements:
  - PH3.0-PROJECTION-SERVICE
  - PH3.0-TENANT-SCOPED-LOADS
  - PH3.0-STRUCTURED-PROJECTION
  - PH3.0-DERIVED-FLAGS
  - PH3.0-READ-ONLY-DISCIPLINE
  - PH3.0-PROJECTION-ENDPOINT
  - PH3.0-PRISMA-FREE-ROUTE
  - PH3.0-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "Service method getWorkflowProjection({ tenantId, ecoId }) exists."
    - "Projection loads ECO-scoped tasks, runtime dependencies, approvals, and gates under tenant scoping."
    - "Projection response includes tasks with state, approvalPolicy, and gate presence."
    - "Projection response includes dependencies with fromTaskId/toTaskId."
    - "Projection reads are batched only: one tasks query, one dependencies query, one approvals-by-taskIds query, one gates-by-taskIds query."
    - "No per-task N+1 query patterns are used."
    - "Projection computes and returns derived flags per task exactly as:"
    - "isBlocked = task.state === \"BLOCKED\""
    - "requiresApproval = task.approvalPolicy !== \"NONE\""
    - "requiresPrecondition = at least one PRECONDITION gate row exists for task"
    - "Projection does not recompute business logic (no dependency/approval/gate condition evaluation)."
    - "Projection is read-only and performs no state mutation."
    - "GET /api/ecos/{id}/projection endpoint exists and delegates to service."
    - "Route remains thin and Prisma-free."
    - "Write-side logic is unchanged."
    - "No background workers are introduced."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "src/services/workflow-projection.service.ts implements tenant-scoped read-model projection."
    - "src/lib/db.ts includes tenant-scoped read helpers required for projection inputs."
    - "src/app/api/ecos/[id]/projection/route.ts exposes projection endpoint and returns service result."
  key_links:
    - "Projection service composes multiple tenantDb reads into one derived response payload."
    - "Derived flags are computed deterministically from persisted state and relationship presence only."
    - "Route only validates input and maps service/errors to HTTP responses."
---

<objective>
Introduce a tenant-scoped workflow projection read model service and endpoint.

Purpose: provide a derived ECO workflow view for clients without modifying write-side behavior or state.
Output: read-only projection service, supporting tenantDb read helpers, and GET /api/ecos/{id}/projection route.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/03.0-workflow-projection-read-model-layer/03.0-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/ecos/[id]/instantiate/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper with projection read helpers</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped read helpers for projection inputs: tasks by ecoId, dependencies by eco task ids (or eco scope through task joins), approvals by task ids, and gates by task ids. Keep helper methods read-only and tenant-filtered.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes all tenant-scoped read primitives needed for projection assembly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement getWorkflowProjection service (read-only)</name>
  <files>src/services/workflow-projection.service.ts</files>
  <action>Create `getWorkflowProjection({ tenantId, ecoId })` service method that performs batched reads only: one tasks query, one dependencies query, one approvals query over taskIds, one gates query over taskIds. Return structured projection including required task fields and derived flags exactly as defined: `isBlocked = task.state === \"BLOCKED\"`, `requiresApproval = task.approvalPolicy !== \"NONE\"`, `requiresPrecondition = existence of at least one PRECONDITION gate row for task`. Do not evaluate dependency satisfaction, approval sufficiency, or gate conditions. Ensure no writes occur and no existing write-side service logic is modified.</action>
  <verify>
    <automated>npm run lint -- src/services/workflow-projection.service.ts || npm run lint</automated>
  </verify>
  <done>Projection service returns deterministic read model payload with required derived fields and no state mutations.</done>
</task>

<task type="auto">
  <name>Task 3: Add projection endpoint and verify thin route behavior</name>
  <files>src/app/api/ecos/[id]/projection/route.ts</files>
  <action>Create `GET /api/ecos/[id]/projection` route. Parse tenantId input (query/header/body convention consistent with existing API style), delegate to projection service, and return response. Keep route Prisma-free and thin. Verify via lint/build/curl path and confirm schema files remain unchanged.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s "http://localhost:3000/api/ecos/REPLACE_ECO_ID/projection?tenantId=REPLACE_TENANT_ID" || true)</automated>
  </verify>
  <done>Projection endpoint returns service-derived workflow read model and preserves architecture constraints.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] getWorkflowProjection({ tenantId, ecoId }) implemented
- [ ] Tasks, dependencies, approvals, and gates are loaded under strict tenant scoping
- [ ] Reads are fully batched (tasks x1, dependencies x1, approvals by taskIds x1, gates by taskIds x1)
- [ ] No per-task N+1 query pattern exists
- [ ] Projection includes tasks with state, approvalPolicy, and gate presence
- [ ] Projection includes dependencies with fromTaskId/toTaskId
- [ ] Derived flags are exactly:
- [ ] isBlocked = task.state === "BLOCKED"
- [ ] requiresApproval = task.approvalPolicy !== "NONE"
- [ ] requiresPrecondition = existence of at least one PRECONDITION gate row for task
- [ ] Projection does not recompute business logic (no dependency/approval/gate condition evaluation)
- [ ] No write operations occur in projection service
- [ ] GET /api/ecos/{id}/projection route exists
- [ ] Route remains thin and Prisma-free
- [ ] Existing write-side logic remains unchanged
- [ ] No background workers introduced
- [ ] `prisma/schema.prisma` unchanged
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Tenant-scoped workflow projection endpoint returns derived read model correctly
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
