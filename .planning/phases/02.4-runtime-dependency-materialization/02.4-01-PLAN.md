---
phase: 02.4-runtime-dependency-materialization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
autonomous: true
requirements:
  - PH2.4-BLUEPRINT-DEPENDENCY-LOAD
  - PH2.4-DEFINITION-TO-TASK-MAPPING
  - PH2.4-RUNTIME-DEPENDENCY-CREATE
  - PH2.4-SELF-DEPENDENCY-REJECTION
  - PH2.4-IDEMPOTENT-NO-DUPLICATES
  - PH2.4-TENANT-ISOLATION
  - PH2.4-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "Instantiation service loads TemplateDependencyDefinition rows scoped by templateVersionId and tenantId."
    - "Service maps fromDefinitionId/toDefinitionId to created task ids using the existing definitionId->taskId mapping."
    - "Runtime dependencies are created with tenant-scoped data: tenantId, fromTaskId, toTaskId, type, lagMinutes."
    - "Service rejects self-dependency when fromTaskId equals toTaskId."
    - "If ECOPlan exists and tasks already exist, service remains idempotent and does not create duplicate dependencies."
    - "Within one instantiation run, duplicate runtime dependency creation is prevented by edge deduplication or existing-row checks on (fromTaskId, toTaskId)."
    - "Structured result includes dependenciesCreated and createdDependencyIds."
    - "No gates or approvals are evaluated in this phase."
    - "No existing tasks are mutated in this phase."
    - "Service/route continue to avoid raw Prisma usage; tenantDb wrapper only."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "src/lib/db.ts exposes tenant-scoped dependency definition read and runtime dependency create/list helpers."
    - "src/services/template-instantiation.service.ts materializes runtime dependencies during blueprint instantiation."
  key_links:
    - "Service uses tenantDb wrapper methods for template dependency definition load and dependency create operations."
    - "Dependency creation happens after parent-first task creation map is complete."
    - "Duplicate-edge safeguards ensure each (fromTaskId, toTaskId) runtime dependency is created at most once per instantiation."
    - "No-op/idempotent branch returns deterministic zero dependency creation when existing tasks are present."
---

<objective>
Materialize runtime Dependency rows from TemplateDependencyDefinition during ECO template instantiation.

Purpose: complete blueprint-driven instantiation by creating tenant-scoped runtime dependency edges without schema changes.
Output: service + tenantDb wrapper updates that create dependencies deterministically and preserve idempotency.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.4-runtime-dependency-materialization/02.4-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/ecos/[id]/instantiate/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper for dependency-definition read and runtime dependency writes</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped wrapper helpers required for this phase only: list `TemplateDependencyDefinition` rows by templateVersionId + tenantId, create runtime `Dependency` rows with tenant-safe task ownership checks, and optionally list existing dependencies for ECO task ids to support deterministic idempotency behavior. Keep all data access in tenantDb wrapper and avoid raw Prisma usage in service/route.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes dependency-definition loading and runtime dependency create/list helpers with tenant-scoped filters.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize runtime dependencies in instantiateTemplateForEco</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Upgrade `instantiateTemplateForEco` to load tenant-scoped `TemplateDependencyDefinition` rows, resolve `fromDefinitionId`/`toDefinitionId` via the existing definitionId->taskId map, and create runtime `Dependency` rows with `tenantId`, `fromTaskId`, `toTaskId`, `type`, and `lagMinutes`. Reject self-dependency at service layer when resolved task ids are equal. Prevent duplicate runtime dependency creation within the same instantiation by either deduplicating blueprint edges before inserts or checking existing rows for `(fromTaskId, toTaskId)` before inserting. Preserve idempotent no-op path: when ECOPlan exists and tasks already exist, do not create duplicate dependencies. Do not mutate existing tasks. Do not evaluate gates or approvals. Return structured result including `dependenciesCreated` and `createdDependencyIds`.</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>Service creates runtime dependencies from blueprint definitions with strict tenant scoping and deterministic idempotency behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Verify endpoint contract passthrough and quality checks</name>
  <files>src/services/template-instantiation.service.ts, src/app/api/ecos/[id]/instantiate/route.ts</files>
  <action>Confirm route remains thin and wrapper/service-only while returning updated structured fields from service (`dependenciesCreated`, `createdDependencyIds`). If route changes are needed, keep them additive and free of Prisma usage. Verify behavior with lint/build/curl checks and confirm no schema files were modified.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/ecos/REPLACE_ECO_ID/instantiate -H 'content-type: application/json' -d '{"tenantId":"REPLACE_TENANT_ID","templateVersionId":"REPLACE_TEMPLATE_VERSION_ID"}' || true)</automated>
  </verify>
  <done>Endpoint response includes dependency materialization result fields through existing service call path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TemplateDependencyDefinition rows are loaded by templateVersionId + tenantId
- [ ] fromDefinitionId/toDefinitionId map correctly to created task ids
- [ ] Runtime dependencies are created with tenantId, fromTaskId, toTaskId, type, lagMinutes
- [ ] Self-dependency is rejected at service layer (fromTaskId == toTaskId)
- [ ] Idempotent path (existing ECOPlan + tasks) does not create duplicate dependencies
- [ ] Duplicate runtime dependencies are prevented within a single instantiation run (edge deduplication or existing-row checks by fromTaskId/toTaskId)
- [ ] Structured result includes dependenciesCreated and createdDependencyIds
- [ ] No gates/approvals evaluated
- [ ] Existing tasks are not mutated
- [ ] Service/route use tenantDb wrapper only; no raw Prisma in service/route
- [ ] `prisma/schema.prisma` unchanged
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Runtime dependency materialization works through existing instantiation flow
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
