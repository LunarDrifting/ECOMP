---
phase: 02.2-blueprint-driven-task-instantiation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/app/api/ecos/[id]/instantiate/route.ts
autonomous: true
requirements:
  - PH2.2-BLUEPRINT-LOAD
  - PH2.2-TENANT-VALIDATION
  - PH2.2-ECOPLAN-FIRST
  - PH2.2-PARENT-FIRST-CREATION
  - PH2.2-PARENT-ID-MAPPING
  - PH2.2-IDEMPOTENCY
  - PH2.2-NO-TASK-MUTATION
  - PH2.2-STRUCTURED-RESULT
  - PH2.2-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "instantiateTemplateForEco loads tenant-scoped TemplateTaskDefinition rows for templateVersionId + tenantId."
    - "TemplateVersion tenant ownership validation remains enforced before task creation."
    - "ECOPlan is ensured before any Task creation."
    - "Tasks are created in deterministic parent-first order from definition graph."
    - "No child task is created before its parent task has been created and mapped."
    - "parentDefinitionId values are correctly mapped to created parentTaskId values."
    - "If ECOPlan exists and tasks already exist for ECO, service returns deterministic conflict or no-op response."
    - "Repeated instantiation calls do not create duplicate task trees."
    - "Blueprint instantiation does not mutate existing tasks."
    - "Result includes ecoPlanId, tasksCreated count, and createdTaskIds array."
    - "Service and route use tenantDb wrapper only; no raw Prisma usage."
    - "No schema/model files are modified."
    - "Gates/approvals are not evaluated in this phase."
  artifacts:
    - "src/services/template-instantiation.service.ts upgraded to blueprint-driven tree creation."
    - "src/lib/db.ts includes tenant-scoped TemplateTaskDefinition load + task create helpers for hierarchy creation."
    - "src/app/api/ecos/[id]/instantiate/route.ts returns updated structured result contract."
  key_links:
    - "Service loads definitions via tenantDb, not direct Prisma."
    - "Definition-to-task map drives parentTaskId assignment."
    - "Route delegates to service and returns structured payload."
---

<objective>
Upgrade template instantiation from a single placeholder task to full blueprint-driven task hierarchy creation.

Purpose: realize TemplateTaskDefinition-based task instantiation while preserving strict tenant isolation and existing schema.
Output: service/db wrapper upgrades and endpoint response updates with deterministic tree creation semantics.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.2-blueprint-driven-task-instantiation/02.2-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/ecos/[id]/instantiate/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb for blueprint definition loading and hierarchical task create</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped helper methods needed for blueprint instantiation: load TemplateTaskDefinition rows by templateVersionId + tenantId (through existing tenant-scoped relations), list existing tasks for ECO under tenant scope, and create task records with optional parentTaskId while enforcing tenant scoping. Keep all new data access in tenantDb wrapper; do not use raw Prisma outside wrapper.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes blueprint definition load and parent-aware task creation methods with tenant-safe constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade instantiateTemplateForEco to blueprint-driven parent-first creation</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Update service logic to: load tenant-scoped definitions, validate TemplateVersion ownership, ensure ECOPlan first, then create tasks in deterministic parent-first order. Implement parent-safe ordering with either: (a) null-parent-first plus iterative child creation, or (b) iterative passes until all parentDefinitionIds resolve. No child task may be created before parent task id exists. Build and use definitionId->taskId map to set parentTaskId. Before creation, enforce idempotency: if ECOPlan already exists and tasks already exist for ECO, return deterministic conflict/no-op response and do not create duplicate trees. Do not mutate existing tasks in this phase. Keep scope to task creation only; do not evaluate gates or approvals. Return structured result: ecoPlanId, tasksCreated, createdTaskIds.</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>Service creates full task hierarchy from definitions with correct parent mapping and structured result fields.</done>
</task>

<task type="auto">
  <name>Task 3: Keep endpoint contract aligned and verify via curl</name>
  <files>src/app/api/ecos/[id]/instantiate/route.ts</files>
  <action>Update endpoint response handling if needed so payload includes structured service result fields. Do not introduce direct Prisma usage in route. Verify with curl against local dev server and capture status/payload showing expected structured shape (including tasksCreated and createdTaskIds). Confirm no schema/model files changed in this phase.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/ecos/REPLACE_ECO_ID/instantiate -H 'content-type: application/json' -d '{\"tenantId\":\"REPLACE_TENANT_ID\",\"templateVersionId\":\"REPLACE_TEMPLATE_VERSION_ID\"}' || true)</automated>
  </verify>
  <done>Endpoint returns structured blueprint-instantiation result and curl path is verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TemplateTaskDefinition rows loaded by templateVersionId + tenantId
- [ ] TemplateVersion tenant ownership validation enforced
- [ ] ECOPlan ensured before Task creation
- [ ] Parent-first deterministic Task creation implemented
- [ ] No child Task created before parent TaskId exists
- [ ] parentDefinitionId -> parentTaskId mapping verified
- [ ] Idempotency enforced when ECOPlan + existing tasks present
- [ ] Repeated instantiation calls do not duplicate tree creation
- [ ] Existing Tasks are not mutated by blueprint instantiation
- [ ] Result includes `ecoPlanId`, `tasksCreated`, `createdTaskIds`
- [ ] No raw Prisma usage in service/route
- [ ] `prisma/schema.prisma` unchanged
- [ ] No gate/approval evaluation logic introduced
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Blueprint-driven instantiation works end-to-end through service + endpoint
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
