---
phase: 02.2-blueprint-driven-task-instantiation
plan: "01"
subsystem: service
tags: [service-layer, tenant-isolation, template-definitions, hierarchy, idempotency]
requires: []
provides:
  - Blueprint-driven task tree creation from TemplateTaskDefinition
  - Deterministic parent-first instantiation with parent mapping
  - Idempotent no-op behavior when ECO already has tasks
affects: [workflow, instantiation, api]
tech-stack:
  added: []
  patterns: [definition-id to task-id mapping for hierarchical creation]
key-files:
  created: []
  modified:
    - src/lib/db.ts
    - src/services/template-instantiation.service.ts
    - src/app/api/ecos/[id]/instantiate/route.ts
key-decisions:
  - "Implemented iterative parent-resolution loop to guarantee parent-first creation and prevent orphan children."
  - "When ECOPlan exists and ECO already has tasks, service returns deterministic no-op response to prevent duplicate tree creation."
patterns-established:
  - "Blueprint instantiation is create-only and does not mutate existing tasks."
  - "All service and route access remains wrapper-based with no direct Prisma in service/route."
requirements-completed:
  - PH2.2-BLUEPRINT-LOAD
  - PH2.2-TENANT-VALIDATION
  - PH2.2-ECOPLAN-FIRST
  - PH2.2-PARENT-FIRST-CREATION
  - PH2.2-PARENT-ID-MAPPING
  - PH2.2-IDEMPOTENCY
  - PH2.2-NO-TASK-MUTATION
  - PH2.2-STRUCTURED-RESULT
  - PH2.2-SCHEMA-STABILITY
duration: 21min
completed: 2026-02-28
---

# Phase 2.2 Plan 01 Summary

**instantiateTemplateForEco now performs blueprint-driven, parent-first task tree creation with deterministic idempotency controls.**

## Performance

- **Duration:** 21 min
- **Started:** 2026-02-28T00:10:00Z
- **Completed:** 2026-02-28T00:31:00Z
- **Tasks:** 3
- **Files modified:** 3

## Accomplishments
- Added tenant-scoped db wrapper helpers for template definition loading, existing ECO task checks, and parent-aware task creation.
- Upgraded `instantiateTemplateForEco` to load `TemplateTaskDefinition` rows and create tasks in deterministic parent-first order with `parentDefinitionId -> parentTaskId` mapping.
- Added deterministic idempotency behavior: returns no-op when ECO already has tasks, preventing duplicate tree creation.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend tenantDb for blueprint definition loading and hierarchical task create** - `5afdfb6` (feat)
2. **Task 2: Upgrade instantiateTemplateForEco to blueprint-driven parent-first creation** - `f3fb8fd` (feat)
3. **Task 3: Keep endpoint contract aligned and verify via curl** - `00e8b29` (feat)

**Verification checkpoint:** `1589989` (chore)

## Files Created/Modified
- `src/lib/db.ts` - Added template definition list, existing task list, and parent-aware task create helpers.
- `src/services/template-instantiation.service.ts` - Added deterministic parent-first hierarchy creation and idempotency no-op logic.
- `src/app/api/ecos/[id]/instantiate/route.ts` - Added 422 mapping for blueprint hierarchy/config errors.

## Decisions Made
- Used iterative passes over pending definitions to guarantee no child creation before parent TaskId exists.
- Chose deterministic no-op behavior (`status: noop_existing_tasks`) when tasks already exist for the ECO.

## Deviations from Plan

None - scope stayed service-layer only with no schema/model changes.

## Issues Encountered
- `npm run lint` reports pre-existing baseline issues in unchanged files (`src/lib/db.ts` historical `any` usage and `src/app/api/admin-only/route.ts` warning).
- Existing running dev server on port 3000 was reused for curl verification (new dev instance could not acquire `.next/dev/lock`).

## Verification Evidence

- Build: `npm run build` passed.
- Seed endpoint: `POST /api/dev/seed-template` returned tenant/templateVersion/eco IDs.
- Instantiation endpoint:
  - `POST /api/ecos/{ecoId}/instantiate` with seeded IDs returned:
    - `status: "noop_existing_tasks"`
    - `tasksCreated: 0`
    - `createdTaskIds: []`
  - Confirms deterministic no-op behavior and non-duplication when tasks already exist.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Blueprint-driven creation path is established.
- Future phases can add richer definition validation/reporting without changing schema.

---
*Phase: 02.2-blueprint-driven-task-instantiation*
*Completed: 2026-02-28*
