---
phase: 03.7-ci-workflow-build-integration-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - PH3.7-CI-WORKFLOW-ADDED
  - PH3.7-PUSH-PR-TRIGGERS
  - PH3.7-TEST-DB-ISOLATION
  - PH3.7-MIGRATE-BUILD-TEST-SEQUENCE
  - PH3.7-NO-DBURL-LEAKAGE
  - PH3.7-DETERMINISTIC-CI
  - PH3.7-NO-SCHEMA-CHANGES
  - PH3.7-NO-PRODUCTION-BEHAVIOR-CHANGE
user_setup: []
must_haves:
  truths:
    - "CI workflow runs on push and pull_request."
    - "Workflow installs dependencies with npm ci."
    - "Workflow uses Node 20 (or project-pinned equivalent) deterministically."
    - "Workflow starts postgres:16 as a GitHub Actions service container."
    - "Workflow creates and uses test DB `ecomp_test_db` for integration tests."
    - "Only DATABASE_URL_TEST is set for migrate/test operations; DATABASE_URL is not used."
    - "Prisma migrate deploy is executed against DATABASE_URL_TEST before build/test."
    - "Build runs before tests in a strict sequential order."
    - "No schema/model files are modified."
    - "No production application behavior is modified."
    - "Execution remains sequential."
  artifacts:
    - ".github/workflows/ci.yml defines checkout, node setup, npm ci, postgres, migrate, build, and test."
    - "CI job env wiring points Prisma and test harness to DATABASE_URL_TEST."
    - "Optional npm cache is configured safely without changing behavior."
  key_links:
    - "DATABASE_URL_TEST fail-fast behavior from existing test harness remains active in CI."
    - "CI database lifecycle is isolated to workflow service container and test DB."
---

<objective>
Add deterministic GitHub Actions CI for build + integration tests.

Purpose:
Guarantee consistent automated verification of build and integration behavior on every push and PR using isolated CI test DB setup.

Output:
Single CI workflow that provisions postgres test DB, migrates, builds, and tests.
</objective>

<execution_context>
Use standard GSD execute workflow and summary template.
</execution_context>

<context>
@.github/workflows/
@package.json
@prisma/schema.prisma
@tests/helpers/test-db.ts
@docs/ARCHITECTURE_CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CI workflow skeleton with deterministic triggers and runtime</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create workflow `ci.yml` triggered on `push` and `pull_request`.
Configure Ubuntu runner, repository checkout, Node setup (pin to 20 unless stricter project version exists), and `npm ci`.
Optionally enable npm cache through `actions/setup-node` for performance while keeping deterministic dependency install behavior.
  </action>
  <verify>
    <automated>yamllint .github/workflows/ci.yml || npx prettier --check .github/workflows/ci.yml</automated>
  </verify>
  <done>Workflow skeleton exists and is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add isolated Postgres test DB provisioning and migrate step</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add postgres:16 service container with health checks.
Add step(s) to create `ecomp_test_db` in CI service.
Set `DATABASE_URL_TEST` (and only DATABASE_URL_TEST) for migration/test steps.
Run `npx prisma migrate deploy` against DATABASE_URL_TEST before build/test steps.
Do not set DATABASE_URL in workflow env.
  </action>
  <verify>
    <automated>grep -n \"DATABASE_URL_TEST\\|DATABASE_URL\" .github/workflows/ci.yml</automated>
  </verify>
  <done>CI uses isolated test DB and migration path without DATABASE_URL leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Run build and integration tests sequentially</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Run `npm run build` then `npm run test` in strict sequence after migrate.
Preserve deterministic ordering and fail-fast behavior.
Do not modify application/service/schema code.
  </action>
  <verify>
    <automated>npm run build && npm run test</automated>
  </verify>
  <done>CI executes deterministic build+test pipeline using test DB.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.github/workflows/ci.yml` added
- [ ] Workflow triggers on push + pull_request
- [ ] Node setup + npm ci present
- [ ] Postgres service uses image `postgres:16`
- [ ] CI creates `ecomp_test_db`
- [ ] Only `DATABASE_URL_TEST` is set for migrate/test
- [ ] `DATABASE_URL` is not used in workflow
- [ ] `npx prisma migrate deploy` runs against DATABASE_URL_TEST
- [ ] `npm run build` runs before `npm run test`
- [ ] No schema/model changes
- [ ] No production behavior changes
</verification>

<success_criteria>
- Every push/PR runs deterministic build + integration tests in CI
- CI test DB isolation is explicit and safe
- Existing workflow engine test harness executes reliably in GitHub Actions
</success_criteria>

<output>
After completion, create the phase summary file for plan "01" in this phase directory.
</output>
