---
phase: 04.1-workflow-projection-enrichment
plan: "01"
subsystem: workflow-projection
tags: [read-model, projection, deterministic-ordering, batched-reads]
requires: []
provides:
  - enriched read-only workflow projection payload
  - actor-aware canComplete signal in projection
  - deterministic graph/topology and aggregate counts in projection
affects: [read-side]
tech-stack:
  added: []
  patterns: [batched read-model composition, deterministic topo fallback, thin route delegation]
key-files:
  created:
    - .planning/phases/04.1-workflow-projection-enrichment/04.1-01-SUMMARY.md
  modified:
    - src/lib/db.ts
    - src/services/workflow-projection.service.ts
    - src/app/api/ecos/[id]/projection/route.ts
    - tests/integration/workflow-engine.integration.test.ts
key-decisions:
  - "Projection remains read-only and does not invoke resolver or write-side flows."
  - "Projection now computes upstream/downstream/blocking/isReady/canComplete fields per task."
  - "Projection now includes deterministic tasksTopologicalOrder with Kahn sort and stable fallback."
  - "Projection now includes total/done/blocked/ready counts derived from projected tasks."
  - "Route accepts optional actorId as query param and remains thin/Prisma-free."
patterns-established:
  - "Single-pass batched query loading for tasks/dependencies/approvals/gates plus optional actor role query."
  - "Deterministic per-task ID-array sorting and topological queue ordering."
requirements-completed:
  - PH4.1-PROJECTION-ENRICHMENT
  - PH4.1-READ-ONLY-DISCIPLINE
  - PH4.1-ACTOR-AWARE-CAN-COMPLETE
  - PH4.1-BATCHED-READS-NO-NPLUS1
  - PH4.1-TOPOLOGICAL-ORDER-DETERMINISM
  - PH4.1-COUNTS-CONSISTENCY
  - PH4.1-TENANT-ISOLATION
  - PH4.1-NO-SCHEMA-CHANGES
duration: 39min
completed: 2026-02-28
---

# Phase 4.1 Plan 01 Summary

**Implemented deterministic, read-only workflow projection enrichment with actor-aware completion eligibility and batched graph metadata.**

## Performance

- **Duration:** 39 min
- **Started:** 2026-02-28T22:20:00Z
- **Completed:** 2026-02-28T22:59:00Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added projection read helpers in `tenantDb`:
  - projection task rows now include `ownerRoleId` + `createdAt`
  - batched gate helper includes `condition` payload for check-only gate evaluation
- Enriched projection service output:
  - per task:
    - `upstreamTaskIds`
    - `downstreamTaskIds`
    - `blockingTaskIds`
    - `isReady`
    - `canComplete` (`null` when actor omitted; boolean when actor provided)
  - global:
    - `tasksTopologicalOrder` (Kahn DAG order with deterministic fallback)
    - `counts` (`totalTasks`, `doneTasks`, `blockedTasks`, `readyTasks`)
  - retained read-only behavior (no writes, no resolver call)
- Updated projection route:
  - accepts optional `actorId` query parameter
  - remains thin and Prisma-free
- Extended integration tests:
  - projection graph enrichment + deterministic ordering/count checks
  - actor-aware `canComplete` checks for BLOCKED/RBAC/approval/gate cases

## Task Commits

1. **Task 1: batched projection read helpers** - `57048f6` (feat)
2. **Task 2: projection enrichment service + thin route input** - `338d4b8` (feat)
3. **Task 3: deterministic projection integration tests** - `d63f7f9` (test)

## Verification Evidence

- `npx eslint src/services/workflow-projection.service.ts src/app/api/ecos/[id]/projection/route.ts tests/integration/workflow-engine.integration.test.ts` passed.
- Integration test execution is environment-dependent on live Postgres connectivity; run command:
  - `DATABASE_URL_TEST=postgresql://postgres:postgres@localhost:5432/ecomp_test_db npm run test -- tests/integration/workflow-engine.integration.test.ts`
- No schema/model files were modified.

## Scope Guardrails Preserved

- No schema changes.
- No write-side behavior changes.
- No background workers.
- Tenant isolation preserved through `tenantDb` scoped reads.

## Next Phase Readiness

- Read model now provides sufficient graph and eligibility metadata for UI consumers without client-side workflow recomputation.

---
*Phase: 04.1-workflow-projection-enrichment*
*Completed: 2026-02-28*
