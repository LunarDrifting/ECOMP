---
phase: 04.1-workflow-projection-enrichment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/workflow-projection.service.ts
  - src/app/api/ecos/[id]/projection/route.ts
  - tests/integration/workflow-engine.integration.test.ts
autonomous: true
requirements:
  - PH4.1-PROJECTION-ENRICHMENT
  - PH4.1-READ-ONLY-DISCIPLINE
  - PH4.1-ACTOR-AWARE-CAN-COMPLETE
  - PH4.1-BATCHED-READS-NO-NPLUS1
  - PH4.1-TOPOLOGICAL-ORDER-DETERMINISM
  - PH4.1-COUNTS-CONSISTENCY
  - PH4.1-TENANT-ISOLATION
  - PH4.1-NO-SCHEMA-CHANGES
user_setup: []
must_haves:
  truths:
    - "Projection remains read-only with no writes or resolver calls."
    - "Tenant isolation is enforced on all projection reads."
    - "Projection adds upstreamTaskIds, downstreamTaskIds, blockingTaskIds, isReady, and canComplete per task."
    - "canComplete returns null when actorId is omitted."
    - "When actorId is provided, canComplete is computed deterministically from state, RBAC, approval policy, and PRECONDITION gates in check-only mode."
    - "Approval policy evaluation for canComplete reuses existing 03.3 semantics and does not mutate state."
    - "Gate evaluation for canComplete reuses existing minimal PRECONDITION evaluator semantics and does not mutate state."
    - "Projection adds global tasksTopologicalOrder with deterministic DAG topo sort and stable fallback ordering."
    - "Projection adds counts: totalTasks, doneTasks, blockedTasks, readyTasks."
    - "Reads are batched: tasks x1, dependencies x1, approvals x1, gates x1, actor roles x1 only when actorId is present."
    - "No per-task N+1 query patterns are introduced."
    - "Route remains thin and Prisma-free while accepting optional actorId parameter."
    - "No write-side behavior is modified."
    - "No schema/model files are modified."
    - "Execution remains sequential."
  artifacts:
    - "src/services/workflow-projection.service.ts computes enriched graph projection and deterministic ordering."
    - "src/lib/db.ts contains batched projection read helpers (including actor role assignments if needed)."
    - "src/app/api/ecos/[id]/projection/route.ts accepts optional actorId and delegates to service."
    - "tests/integration/workflow-engine.integration.test.ts includes deterministic projection enrichment tests."
  key_links:
    - "Projection payload reflects persisted state and relationship presence only (no write-side recomputation/mutation)."
    - "tasksTopologicalOrder uses Kahn algorithm with deterministic queue ordering."
    - "Fallback ordering is createdAt then id for stable output if runtime graph has cycle."
---

<objective>
Enrich workflow projection for full graph rendering while preserving read-only, batched, deterministic service behavior.

Purpose:
Allow UI/consumers to render actionable workflow graph state without client-side engine recomputation.

Output:
Enriched projection service/route with deterministic graph metadata and integration test coverage.
</objective>

<execution_context>
Use standard GSD execute workflow and summary template.
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@src/lib/db.ts
@src/services/workflow-projection.service.ts
@src/services/template-instantiation.service.ts
@src/app/api/ecos/[id]/projection/route.ts
@tests/integration/workflow-engine.integration.test.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batched projection read helpers for graph + actor context</name>
  <files>src/lib/db.ts</files>
  <action>
Add/extend tenantDb read helpers required by projection enrichment while preserving tenant scope and read-only behavior:
- tasks for eco (including fields needed for deterministic ordering and canComplete checks)
- dependencies by taskIds
- approvals by taskIds (policy evaluation shape)
- PRECONDITION gates by taskIds (condition payload)
- actor role assignments by actorId (only used when actorId provided)
Keep helper contracts batched and avoid per-task helper calls.
  </action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes all batched read inputs needed for enriched projection.</done>
</task>

<task type="auto">
  <name>Task 2: Implement enriched projection service with deterministic graph metadata</name>
  <files>src/services/workflow-projection.service.ts</files>
  <action>
Update `getWorkflowProjection` to accept optional `actorId` and return enriched task fields:
- upstreamTaskIds
- downstreamTaskIds
- blockingTaskIds
- isReady
- canComplete (null or boolean per rules)
Add global projection fields:
- tasksTopologicalOrder (Kahn deterministic, cycle fallback to createdAt/id sort)
- counts (totalTasks, doneTasks, blockedTasks, readyTasks)
Rules:
- Read-only only
- No resolver calls
- No state mutation
- Reuse existing approval policy semantics in check-only mode
- Reuse PRECONDITION minimal evaluator in check-only mode
- Deterministic sorting of task ID arrays
- Strict batched loading (no N+1)
  </action>
  <verify>
    <automated>npm run lint -- src/services/workflow-projection.service.ts || npm run lint</automated>
  </verify>
  <done>Projection service returns deterministic enriched graph read model with actor-aware canComplete.</done>
</task>

<task type="auto">
  <name>Task 3: Keep projection route thin and add deterministic integration tests</name>
  <files>src/app/api/ecos/[id]/projection/route.ts, tests/integration/workflow-engine.integration.test.ts</files>
  <action>
Route updates:
- accept optional `actorId` query parameter
- keep route thin and Prisma-free
- delegate fully to projection service
Test updates (add at least 2 integration tests):
1) projection returns upstream/downstream/blocking/isReady and counts consistent with tasks
2) projection with actorId computes canComplete correctly:
   - false for BLOCKED task
   - true/false based on RBAC + approval policy + gate checks using deterministic fixtures
Ensure deterministic assertions for ordering (`tasksTopologicalOrder` and sorted task ID arrays).
  </action>
  <verify>
    <automated>npm run test -- tests/integration/workflow-engine.integration.test.ts && npm run build</automated>
  </verify>
  <done>Thin projection route and deterministic integration coverage are complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No schema/model changes
- [ ] Projection path remains read-only (no writes, no resolver calls)
- [ ] Tenant isolation enforced across all projection reads
- [ ] Batched reads only: tasks x1, dependencies x1, approvals x1, gates x1
- [ ] Actor role assignments loaded in one additional query only when actorId is provided
- [ ] No per-task N+1 read patterns
- [ ] Task projection includes upstreamTaskIds
- [ ] Task projection includes downstreamTaskIds
- [ ] Task projection includes blockingTaskIds
- [ ] Task projection includes isReady
- [ ] Task projection includes canComplete with null when actorId omitted
- [ ] canComplete uses deterministic check-only logic for state/RBAC/approval/gates
- [ ] tasksTopologicalOrder returned and deterministic
- [ ] cycle fallback ordering uses createdAt then id
- [ ] counts.totalTasks equals tasks.length
- [ ] counts.doneTasks/blockedTasks/readyTasks consistent with projected task data
- [ ] Route remains thin and Prisma-free
- [ ] At least two integration tests added for enrichment shape + canComplete behavior
- [ ] `npm run build` passes
</verification>

<success_criteria>
- Projection provides complete deterministic graph state for consumers
- Actor-aware completion eligibility is available as read-only signal
- Read-side enrichment ships without schema or write-side behavior changes
</success_criteria>

<output>
After completion, create the phase summary file for plan "01" in this phase directory.
</output>
