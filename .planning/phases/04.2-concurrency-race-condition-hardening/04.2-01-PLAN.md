---
phase: 04.2-concurrency-race-condition-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/workflow-engine.integration.test.ts
  - src/services/template-instantiation.service.ts
  - src/lib/db.ts
autonomous: true
requirements:
  - PH4.2-CONCURRENT-COMPLETION-CORRECTNESS
  - PH4.2-SAME-TASK-SINGLE-WRITER-BEHAVIOR
  - PH4.2-DOWNSTREAM-UNBLOCK-ONCE
  - PH4.2-BLOCKED-CONCURRENT-REJECTION
  - PH4.2-TX-ATOMICITY-PRESERVED
  - PH4.2-DETERMINISTIC-ASSERTIONS
  - PH4.2-MINIMAL-HARDENING-ONLY-IF-NEEDED
  - PH4.2-NO-SCHEMA-CHANGES
user_setup: []
must_haves:
  truths:
    - "Concurrency validation is test-first: prove current behavior before any hardening changes."
    - "markTaskDone keeps single-transaction atomic boundary for DONE + cascade behavior."
    - "Concurrent completion of same NOT_STARTED task yields exactly-one effective DONE mutation."
    - "Concurrent completion race does not create duplicate downstream unblocks."
    - "Concurrent completion of two upstream prerequisites unblocks shared downstream exactly once."
    - "Concurrent BLOCKED completion attempts remain deterministic rejection."
    - "Tenant isolation, RBAC, approval, gate, and transition guardrail semantics remain preserved."
    - "No sleeps/timeouts or timing-dependent assertions are used."
    - "If hardening is required, it is minimal and localized (service/db helper only)."
    - "No schema/model files are modified."
    - "Execution remains sequential."
  artifacts:
    - "tests/integration/workflow-engine.integration.test.ts contains 2-3 deterministic concurrency tests."
    - "src/services/template-instantiation.service.ts includes concurrency hardening only if tests expose races."
    - "src/lib/db.ts includes atomic conditional helper only if needed by hardening."
  key_links:
    - "Concurrent verification uses Promise.all against completion entrypoint(s) and asserts end-state in DB."
    - "Cascade resolver remains within transaction scope and uses tx-visible state."
    - "Duplicate unblock prevention is proven by state + response payload determinism."
---

<objective>
Validate and harden workflow completion concurrency behavior with deterministic, transaction-safe guarantees.

Purpose:
Ensure race conditions cannot violate correctness when completion requests overlap.

Output:
Concurrency integration coverage and minimal hardening (only if tests require it), with no schema changes.
</objective>

<execution_context>
Use standard GSD execute workflow and summary template.
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@src/services/template-instantiation.service.ts
@src/lib/db.ts
@src/services/state-transition.service.ts
@src/app/api/tasks/[id]/complete/route.ts
@tests/integration/workflow-engine.integration.test.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic concurrency integration tests (test-first)</name>
  <files>tests/integration/workflow-engine.integration.test.ts</files>
  <action>
Add 2-3 deterministic integration tests that execute concurrent completion calls via Promise.all:
1) same NOT_STARTED task completed concurrently:
- assert exactly one effective DONE mutation
- assert losing response is deterministic no-op/conflict class
- assert no duplicate downstream unblocking effects
2) two upstream tasks (same downstream dependency target) completed concurrently:
- assert both upstream end DONE
- assert downstream becomes NOT_STARTED exactly once in final state
- assert no duplicate unblock behavior in responses/state
3) optional/required blocked-task concurrency:
- concurrent completion attempts on BLOCKED task remain deterministic rejection
Assertions must rely on response payloads + DB end state only (no time-based checks).
  </action>
  <verify>
    <automated>npm run test -- tests/integration/workflow-engine.integration.test.ts</automated>
  </verify>
  <done>Concurrency scenarios are covered and deterministically asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Apply minimal concurrency hardening only if tests expose a race</name>
  <files>src/services/template-instantiation.service.ts, src/lib/db.ts</files>
  <action>
Only if Task 1 reveals race bugs, implement minimal hardening:
- Prefer atomic conditional DONE update semantics (single-writer) in tx
- Or row-level lock approach only if strictly necessary
- Keep cascade resolver in same transaction using tx-bound client
- Preserve all existing enforcement checks and ordering
- Preserve idempotent deterministic behavior for loser path
Do not modify behavior beyond concurrency safety and determinism.
  </action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts src/lib/db.ts && npm run test -- tests/integration/workflow-engine.integration.test.ts</automated>
  </verify>
  <done>Concurrency race conditions are closed with minimal transactional hardening, if required.</done>
</task>

<task type="auto">
  <name>Task 3: Re-verify deterministic end-state guarantees under concurrent calls</name>
  <files>tests/integration/workflow-engine.integration.test.ts, src/services/template-instantiation.service.ts</files>
  <action>
Re-run concurrency tests and validate deterministic outcomes:
- exactly-one behavior on same-task concurrent completion
- downstream unblocks once for shared-prerequisite race
- blocked-task concurrent attempts all reject deterministically
- no duplicate dependency/state side effects
Document any behavior clarification (no-op vs conflict) in summary as deterministic contract.
  </action>
  <verify>
    <automated>npm run test -- tests/integration/workflow-engine.integration.test.ts && npm run build</automated>
  </verify>
  <done>Deterministic concurrency contract is proven and stable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No schema/model changes
- [ ] Concurrency coverage added with Promise.all execution
- [ ] Same-task concurrent completion shows exactly-one effective DONE mutation
- [ ] Losing concurrent result is deterministic (no-op or conflict contract)
- [ ] No duplicate downstream unblocks occur for same-task race
- [ ] Concurrent upstream completion of shared downstream yields single deterministic unblock effect
- [ ] Concurrent BLOCKED completion attempts are deterministic rejection
- [ ] Assertions are DB end-state and payload based (no timing dependence)
- [ ] markTaskDone transaction boundary remains atomic
- [ ] If hardening added, it is minimal and localized
- [ ] Tenant isolation/RBAC/approval/gate/transition semantics preserved
- [ ] `npm run build` passes
</verification>

<success_criteria>
- Workflow completion remains correct under concurrent requests
- Any required hardening is minimal and transaction-safe
- Deterministic concurrency behavior is proven by integration tests
</success_criteria>

<output>
After completion, create the phase summary file for plan "01" in this phase directory.
</output>
