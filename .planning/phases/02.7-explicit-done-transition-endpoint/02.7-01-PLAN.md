---
phase: 02.7-explicit-done-transition-endpoint
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/app/api/tasks/[id]/complete/route.ts
autonomous: true
requirements:
  - PH2.7-MARK-TASK-DONE-SERVICE
  - PH2.7-TENANT-TASK-VALIDATION
  - PH2.7-DONE-STATE-UPDATE
  - PH2.7-RESOLVER-INTEGRATION
  - PH2.7-STRUCTURED-RESULT
  - PH2.7-COMPLETE-ENDPOINT
  - PH2.7-PRISMA-FREE-ROUTE
  - PH2.7-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "Service method markTaskDone({ tenantId, taskId }) exists."
    - "markTaskDone validates task exists within tenant scope."
    - "markTaskDone validates task is not already DONE before mutating state."
    - "markTaskDone updates task state to DONE via tenant-scoped wrapper operations."
    - "markTaskDone calls resolveDependenciesForTask({ tenantId, taskId }) after marking DONE."
    - "Result includes taskMarkedDone, tasksUnblocked, and unblockedTaskIds."
    - "Canonical endpoint POST /api/tasks/{id}/complete exists and delegates to service."
    - "Route remains thin and does not use raw Prisma."
    - "No approvals/gates are evaluated in this phase."
    - "No background workers are introduced."
    - "Idempotency is preserved with deterministic behavior for already-DONE tasks."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "src/services/template-instantiation.service.ts includes markTaskDone and calls existing standalone resolver."
    - "src/lib/db.ts includes tenant-scoped task lookup/update helper(s) required by markTaskDone."
    - "src/app/api/tasks/[id]/complete/route.ts delegates to service and returns structured completion payload."
  key_links:
    - "markTaskDone composes DONE state update + resolveDependenciesForTask in service layer."
    - "Route only validates input, calls service, and maps errors to HTTP responses."
    - "Resolver remains standalone and manually invocable; completion endpoint calls it explicitly."
---

<objective>
Introduce a canonical DONE transition endpoint and service method that triggers runtime dependency resolution.

Purpose: provide an explicit, tenant-safe completion path that updates a task to DONE and reports downstream unblocking results.
Output: service/db wrapper updates plus a new thin API route at POST /api/tasks/{id}/complete.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.7-explicit-done-transition-endpoint/02.7-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/ecos/[id]/instantiate/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper for DONE transition primitives</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped wrapper primitives needed for markTaskDone: lookup task by id/state under tenant scope and update a single task state to DONE deterministically. Keep all data access wrapper-contained and tenant-filtered. No raw Prisma in route/service.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes tenant-scoped task lookup and DONE update operations for completion workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Implement markTaskDone service method and resolver handoff</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Add `markTaskDone({ tenantId, taskId })` to service layer. Validate task exists for tenant and is not already DONE, update state to DONE, then call `resolveDependenciesForTask({ tenantId, taskId })`. Return structured result including `taskMarkedDone`, `tasksUnblocked`, and `unblockedTaskIds`. Preserve deterministic idempotent behavior for repeated completion attempts.</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>Service method performs canonical DONE transition and dependency resolution with structured output.</done>
</task>

<task type="auto">
  <name>Task 3: Add canonical completion endpoint and verify route thinness</name>
  <files>src/app/api/tasks/[id]/complete/route.ts</files>
  <action>Create `POST /api/tasks/[id]/complete` route that parses tenantId payload, calls `markTaskDone`, and returns structured result. Keep route Prisma-free and thin. Map expected validation/state conflict errors to deterministic HTTP responses. Run lint/build/curl checks and confirm no schema/model changes.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/tasks/REPLACE_TASK_ID/complete -H 'content-type: application/json' -d '{"tenantId":"REPLACE_TENANT_ID"}' || true)</automated>
  </verify>
  <done>Completion endpoint exists, delegates to service only, and returns expected completion + unblocking payload.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `markTaskDone({ tenantId, taskId })` exists in service layer
- [ ] Task existence is validated under tenant scope
- [ ] Already-DONE task handling is deterministic and idempotent
- [ ] Task.state is updated to DONE under tenant-scoped wrapper
- [ ] `resolveDependenciesForTask({ tenantId, taskId })` is called after DONE update
- [ ] Result includes `taskMarkedDone`, `tasksUnblocked`, and `unblockedTaskIds`
- [ ] `POST /api/tasks/{id}/complete` route exists
- [ ] Route is thin and Prisma-free
- [ ] No approvals/gates are evaluated
- [ ] No background workers are introduced
- [ ] `prisma/schema.prisma` unchanged
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- DONE transition endpoint and service work with dependency resolver integration
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
