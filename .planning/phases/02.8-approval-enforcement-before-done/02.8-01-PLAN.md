---
phase: 02.8-approval-enforcement-before-done
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/app/api/tasks/[id]/complete/route.ts
autonomous: true
requirements:
  - PH2.8-APPROVAL-POLICY-CHECK
  - PH2.8-APPROVAL-ROW-VALIDATION
  - PH2.8-APPROVAL-REJECTION-NO-MUTATION
  - PH2.8-RESOLVER-GUARDED-CALL
  - PH2.8-IDEMPOTENCY-PRESERVATION
  - PH2.8-STRUCTURED-RESULT
  - PH2.8-TENANT-ISOLATION
  - PH2.8-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "markTaskDone enforces approval requirements before DONE transition."
    - "If approvalPolicy is NONE, completion path remains allowed (subject to existing BLOCKED/already-DONE checks)."
    - "For non-NONE approvalPolicy values (SINGLE, SEQUENTIAL, PARALLEL, QUORUM), markTaskDone applies the same check: at least one tenant-scoped APPROVED decision is required."
    - "No ordering, quorum-threshold, or role-based approval constraints are enforced in this phase."
    - "No rejection semantics beyond simple block on missing APPROVED row are introduced in this phase."
    - "If no APPROVED approval exists, markTaskDone rejects deterministically with 409 behavior."
    - "Approval rejection performs no state mutation and does not call resolveDependenciesForTask."
    - "Allowed completion still updates DONE state and then calls resolveDependenciesForTask."
    - "Existing BLOCKED rejection and already-DONE idempotency are preserved."
    - "Result includes taskMarkedDone, tasksUnblocked, unblockedTaskIds, approvalCheckPassed."
    - "Route remains thin and Prisma-free."
    - "No gate evaluation is introduced in this phase."
    - "No background workers are introduced."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "src/lib/db.ts includes tenant-scoped approval lookup helper(s) for markTaskDone checks."
    - "src/services/template-instantiation.service.ts updates markTaskDone with approval enforcement and approvalCheckPassed result field."
    - "src/app/api/tasks/[id]/complete/route.ts returns deterministic response/error mapping for approval rejections."
  key_links:
    - "markTaskDone performs approval gate before DONE mutation for non-NONE policies."
    - "resolveDependenciesForTask is only called on successful DONE transitions."
    - "Deterministic idempotent/no-op semantics remain for already-DONE tasks."
    - "Full policy-specific approval semantics are explicitly deferred to a later phase."
---

<objective>
Enforce approval requirements in markTaskDone before DONE transitions.

Purpose: prevent completion of approval-required tasks without approved decisions while preserving existing dependency/idempotency safeguards.
Output: service/db wrapper updates and endpoint response integration with approval-aware completion result.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.8-approval-enforcement-before-done/02.8-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/tasks/[id]/complete/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper for tenant-scoped approval checks</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped wrapper helper(s) to load Approval rows by taskId and filter/inspect decisions needed for markTaskDone enforcement. Keep data access wrapper-only and tenant-filtered.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes tenant-safe approval lookup primitives for completion validation.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce approval policy in markTaskDone</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Update markTaskDone to enforce approval logic: allow when approvalPolicy is NONE; otherwise (for SINGLE, SEQUENTIAL, PARALLEL, QUORUM) apply identical minimum enforcement requiring at least one tenant-scoped APPROVED Approval row for the task. Do not implement ordering, quorum thresholds, or role-based approval constraints in this phase. If requirement fails, return deterministic conflict behavior and do not mutate state or call resolver. If requirement passes, proceed with existing DONE update + resolver call. Preserve BLOCKED rejection and already-DONE idempotent no-op behavior. Add `approvalCheckPassed` to structured result payload.</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>markTaskDone enforces approvals pre-DONE and returns approval-aware structured result.</done>
</task>

<task type="auto">
  <name>Task 3: Keep completion endpoint thin with deterministic approval error mapping</name>
  <files>src/app/api/tasks/[id]/complete/route.ts</files>
  <action>Update completion route error mapping as needed to expose deterministic 409 for approval enforcement rejection while staying Prisma-free and thin. Ensure success response includes new `approvalCheckPassed` field from service result. Run lint/build/curl checks and confirm no schema/model changes.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/tasks/REPLACE_TASK_ID/complete -H 'content-type: application/json' -d '{"tenantId":"REPLACE_TENANT_ID"}' || true)</automated>
  </verify>
  <done>Route preserves thin-service pattern and deterministic approval rejection behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Approval policy check enforced in markTaskDone
- [ ] All non-NONE policies (SINGLE, SEQUENTIAL, PARALLEL, QUORUM) are treated identically in this phase
- [ ] Non-NONE policy requires at least one APPROVED approval row (tenant-scoped)
- [ ] No ordering, quorum-threshold, or role-based approval constraints are implemented in this phase
- [ ] Full policy-specific semantics are explicitly deferred to a later phase
- [ ] Missing APPROVED approval returns deterministic 409 behavior
- [ ] Approval rejection performs no state mutation
- [ ] Approval rejection does not call resolveDependenciesForTask
- [ ] Allowed path still performs DONE update then resolver call
- [ ] Existing BLOCKED rejection and already-DONE idempotency preserved
- [ ] Result includes taskMarkedDone, tasksUnblocked, unblockedTaskIds, approvalCheckPassed
- [ ] Route remains thin and Prisma-free
- [ ] No gate evaluation introduced
- [ ] No background workers introduced
- [ ] `prisma/schema.prisma` unchanged
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Approval enforcement prevents unauthorized DONE transitions
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
