---
phase: 02.6-runtime-dependency-resolution-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
autonomous: true
requirements:
  - PH2.6-RESOLVE-SERVICE-METHOD
  - PH2.6-DOWNSTREAM-LOOKUP
  - PH2.6-INCOMING-DEPENDENCY-CHECK
  - PH2.6-BLOCKED-ONLY-UNBLOCK
  - PH2.6-TARGETED-REEVALUATION
  - PH2.6-STRUCTURED-RESULT
  - PH2.6-TENANT-ISOLATION
  - PH2.6-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "Service method resolveDependenciesForTask({ tenantId, taskId }) exists."
    - "resolveDependenciesForTask is a standalone, manually invocable service method in this phase."
    - "Resolver is not auto-triggered during instantiation."
    - "Resolver loads downstream tasks from runtime Dependency rows using the completed task as upstream source."
    - "For affected downstream tasks, resolver checks incoming runtime dependencies against upstream Task.state values loaded in batch (no N+1 query pattern)."
    - "A downstream task is set to NOT_STARTED only when all its upstream runtime dependencies are DONE and it is currently BLOCKED."
    - "Tasks not currently BLOCKED are never mutated by resolver."
    - "Resolver re-evaluates only affected downstream tasks, not the whole graph."
    - "Resolver returns structured result with tasksUnblocked and unblockedTaskIds."
    - "No gates/approvals are evaluated."
    - "No background workers or async orchestration are introduced."
    - "Service/route use tenantDb wrapper only; no raw Prisma in service/route."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "src/services/template-instantiation.service.ts (or another existing service file) includes standalone resolveDependenciesForTask service method."
    - "src/lib/db.ts includes tenant-scoped dependency/task query helpers needed for targeted downstream resolution."
  key_links:
    - "Resolver operates on runtime Dependency edges (fromTaskId/toTaskId), not blueprint definitions."
    - "Only direct downstream tasks of the completed task are checked in this phase scope."
    - "Upstream Task.state values are loaded in batch via tenantDb wrapper methods."
    - "Repeated calls preserve idempotent behavior by mutating BLOCKED tasks only when conditions are satisfied."
---

<objective>
Implement runtime dependency resolution so completed tasks can unblock ready downstream tasks.

Purpose: introduce targeted downstream dependency checks that transition BLOCKED tasks to NOT_STARTED when all upstream dependencies are DONE.
Output: service/db wrapper updates exposing deterministic standalone downstream unblocking results.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.6-runtime-dependency-resolution-engine/02.6-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper for targeted downstream dependency resolution queries</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped wrapper helpers to: fetch downstream dependency edges for a given taskId, fetch incoming dependencies for candidate downstream tasks, fetch required upstream task states in batch for incoming edges (no N+1), and update a downstream task state when needed. Keep all data access tenant-scoped and wrapper-contained; no raw Prisma in service/route.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes all tenant-scoped primitives required for focused downstream dependency resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Implement standalone resolveDependenciesForTask service method</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Add standalone `resolveDependenciesForTask({ tenantId, taskId })` service method in an existing service file. When manually called after a task is marked DONE, load direct downstream tasks via runtime dependency rows, check all incoming runtime dependencies for each downstream task using batched upstream state loads, and set state to NOT_STARTED only if the task is currently BLOCKED and all upstream tasks are DONE. Do not mutate non-BLOCKED tasks. Do not re-evaluate whole graph. Preserve idempotent behavior for repeated calls. Ensure result includes `tasksUnblocked` and `unblockedTaskIds`. Do not auto-integrate this resolver into instantiation flow in this phase.</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>Resolver is implemented as standalone service method with correct downstream-only behavior and structured result.</done>
</task>

<task type="auto">
  <name>Task 3: Verify standalone resolver contract and service-level checks</name>
  <files>src/services/template-instantiation.service.ts, src/lib/db.ts</files>
  <action>Ensure resolver contract returns `tasksUnblocked` and `unblockedTaskIds`, confirm upstream state checks are batch-loaded via tenantDb wrapper, and verify resolver remains manually invocable (not auto-triggered by instantiation). Verify with lint/build and confirm no schema/model files are modified.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>Standalone service-level resolver reports deterministic dependency-resolution results while preserving architecture constraints.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `resolveDependenciesForTask({ tenantId, taskId })` exists in service layer
- [ ] Resolver is standalone/manual and not auto-triggered during instantiation
- [ ] Resolver uses runtime dependency rows to load affected downstream tasks
- [ ] Resolver checks all incoming runtime dependencies per downstream task using batched upstream state loads (no N+1)
- [ ] Downstream task is unblocked only when currently BLOCKED and all upstream tasks are DONE
- [ ] Non-BLOCKED tasks are never mutated
- [ ] Resolver re-evaluates only affected downstream tasks (no full-graph pass)
- [ ] Result includes `tasksUnblocked` and `unblockedTaskIds`
- [ ] No gates/approvals evaluated
- [ ] No background workers/async orchestration introduced
- [ ] Service/route use tenantDb wrapper only; no raw Prisma in service/route
- [ ] `prisma/schema.prisma` unchanged
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- DONE transitions can unblock eligible downstream tasks deterministically
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
