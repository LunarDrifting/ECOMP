---
phase: 03.2-role-based-permission-enforcement-layer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/services/approval.service.ts
  - src/app/api/tasks/[id]/complete/route.ts
  - src/app/api/tasks/[id]/approvals/route.ts
autonomous: true
requirements:
  - PH3.2-MARK-DONE-AUTHZ
  - PH3.2-APPROVAL-CREATE-AUTHZ
  - PH3.2-403-DETERMINISTIC-REJECTION
  - PH3.2-TENANT-ISOLATION
  - PH3.2-IDEMPOTENCY-PRESERVATION
  - PH3.2-ROUTE-THINNESS
  - PH3.2-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "markTaskDone accepts actorId and enforces tenant-scoped authorization before mutation."
    - "Only ADMIN role or task.ownerRoleId role can mark a task DONE."
    - "Authorization failures are rejected deterministically and map to 403."
    - "Existing BLOCKED rejection, approval enforcement, and precondition gate enforcement order remains preserved after auth check insertion."
    - "Already-DONE idempotent no-op remains preserved with no resolver invocation and no mutation."
    - "Approval creation path enforces actor role must match task.ownerRoleId."
    - "Unauthorized approval creation is rejected with deterministic 403 and no writes."
    - "All reads/writes are tenant-scoped via tenantDb wrapper only."
    - "Projection layer remains unchanged."
    - "Instantiation flow remains unchanged."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave phase."
  artifacts:
    - "src/services/template-instantiation.service.ts updated with actor-based completion authorization in markTaskDone."
    - "src/services/approval.service.ts provides approval creation with service-layer authorization guard."
    - "src/app/api/tasks/[id]/complete/route.ts requires actorId and maps authorization failures to 403."
    - "src/app/api/tasks/[id]/approvals/route.ts remains thin/Prisma-free and maps authorization failures to 403."
    - "src/lib/db.ts exposes tenant-scoped role lookup and approval creation helpers required by services."
  key_links:
    - "Authorization checks execute before any state mutation."
    - "No write-side behavior changes outside authorization gates."
    - "Deterministic error mapping is centralized in thin routes."
---

<objective>
Implement deterministic tenant-scoped RBAC enforcement for task completion and approval creation.

Purpose: prevent unauthorized task DONE transitions and approvals while preserving existing dependency/approval/gate behaviors.
Output: service-level authorization guards, thin route updates, deterministic 403 error mapping.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/03.2-role-based-permission-enforcement-layer/03.2-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/tasks/[id]/complete/route.ts
@src/services/workflow-projection.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper for tenant-scoped authorization and approval writes</name>
  <files>src/lib/db.ts</files>
  <action>Add/adjust tenant-scoped helpers for: actor role membership lookup, task ownerRole lookup in completion path, and approval row creation used by service layer. Keep all queries batched or scoped and avoid raw Prisma usage in routes/services.</action>
  <verify>
    <automated>npx eslint src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes all required role/approval helpers for deterministic auth enforcement.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce completion authorization in markTaskDone without changing existing write semantics</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Update markTaskDone signature to require actorId. Resolve actor roles under tenant scope and allow DONE transition only for ADMIN or ownerRole match. Reject unauthorized with deterministic auth error before mutation. Preserve existing BLOCKED rejection, already-DONE idempotency behavior, approval enforcement, gate enforcement, and dependency resolver behavior.</action>
  <verify>
    <automated>npx eslint src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>Task completion is tenant-scoped and role-authorized with deterministic failure and unchanged downstream semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add service-level approval creation guard using owner-role authorization</name>
  <files>src/services/approval.service.ts, src/lib/db.ts</files>
  <action>Create approval service method that validates task exists in tenant, loads actor roles in tenant scope, enforces actor role match to task.ownerRoleId, and only then creates approval row. Keep approval policy semantics unchanged (this phase gates creation authorization only).</action>
  <verify>
    <automated>npx eslint src/services/approval.service.ts src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>Approval creation is blocked for unauthorized actors and allowed for owner-role actors under tenant scope.</done>
</task>

<task type="auto">
  <name>Task 4: Update task completion route and add thin approval creation route with 403 mapping</name>
  <files>src/app/api/tasks/[id]/complete/route.ts, src/app/api/tasks/[id]/approvals/route.ts</files>
  <action>Require actorId for completion payload and pass to markTaskDone. Add/update route-level error mapping to return deterministic 403 for authorization failures. Add thin Prisma-free approval creation endpoint delegating to service and mapping authorization failures to 403. Keep route logic minimal and deterministic.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/tasks/REPLACE_TASK_ID/complete -H 'content-type: application/json' -d '{"tenantId":"REPLACE_TENANT_ID","actorId":"REPLACE_USER_ID"}' || true)</automated>
  </verify>
  <done>Routes are thin, Prisma-free, and return deterministic 403 for authorization failures.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] markTaskDone accepts actorId
- [ ] markTaskDone authorization allows ADMIN role completion
- [ ] markTaskDone authorization allows owner-role completion
- [ ] Unauthorized markTaskDone attempts reject deterministically with 403
- [ ] Authorization checks occur before mutation in completion flow
- [ ] BLOCKED rejection still enforced
- [ ] Already-DONE no-op still preserved with no mutation
- [ ] Approval enforcement semantics unchanged
- [ ] Gate enforcement semantics unchanged
- [ ] Approval creation path enforces owner-role authorization
- [ ] Unauthorized approval creation rejects deterministically with 403
- [ ] Authorized approval creation succeeds under tenant scope
- [ ] All reads/writes are via tenantDb wrapper only
- [ ] Projection layer unchanged
- [ ] Instantiation flow unchanged
- [ ] No schema/model changes
- [ ] `npm run lint` completes (report baseline failures if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- Deterministic tenant-scoped authorization enforced for completion + approval creation
- 403 mapping applied consistently to authorization failures
- Existing write-side business semantics preserved outside authorization gates
- Phase remains sequential with a single wave
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
