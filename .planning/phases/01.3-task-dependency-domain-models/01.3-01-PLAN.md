---
phase: 01.3-task-dependency-domain-models
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_task_dependency_models/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: true
requirements:
  - PH1.3-TASK-MODEL
  - PH1.3-DEPENDENCY-MODEL
  - PH1.3-SCHEMA-ONLY-DISCIPLINE
user_setup: []
must_haves:
  truths:
    - "Prisma schema defines Task and Dependency models with required fields and enums from phase scope."
    - "Task and Dependency relations enforce tenant-safe ownership paths through existing tenant-scoped entities."
    - "A Prisma migration is generated and applies without direct database edits."
    - "No application route/service business logic is added or modified in this phase."
  artifacts:
    - "prisma/schema.prisma includes new enums and models: Task, Dependency."
    - "prisma/migrations/*_add_task_dependency_models/migration.sql creates corresponding tables, FKs, and indexes."
  key_links:
    - "Task.ecoId references ECO.id; Task.ownerRoleId references Role.id."
    - "Task.parentTaskId self-references Task.id as nullable hierarchy link."
    - "Dependency.fromTaskId and Dependency.toTaskId both reference Task.id."
---

<objective>
Add Task and Dependency Prisma domain models aligned to the Preliminary Design Spec while preserving current architecture boundaries.

Purpose: establish workflow data-model foundation without introducing business logic.
Output: updated `prisma/schema.prisma` and a Prisma migration for new tables/relations/enums only.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/01.3-task-dependency-domain-models/01.3-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Task/Dependency enums and models to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <action>Add Prisma enums and models required by phase scope only. Include Task fields: ecoId, parentTaskId (nullable), taskLevel enum, name, ownerRoleId, state enum, visibility enum, approvalPolicy enum, clockMode enum, createdAt, updatedAt. Include Dependency fields: fromTaskId, toTaskId, type enum (finish_to_start/start_to_start), lagMinutes, plus timestamps if needed for consistency. Wire relations: Task->ECO, Task->Role, Task self-parent, Dependency task-edge relations. Keep tenant scoping where applicable using tenant ownership path through ECO/Role and relation constraints. Do not modify route files, service files, or existing model semantics.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema parses successfully and contains all required Task/Dependency fields and enums with explicit relations.</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for Task/Dependency schema changes</name>
  <files>prisma/migrations/*_add_task_dependency_models/migration.sql, prisma/migrations/migration_lock.toml</files>
  <action>Run Prisma migration creation for the new schema (`npx prisma migrate dev --name add_task_dependency_models`). Confirm generated SQL creates Task/Dependency tables, enum usage, foreign keys, and non-destructive changes only. Do not hand-edit database directly.</action>
  <verify>
    <automated>npx prisma migrate dev --name add_task_dependency_models</automated>
  </verify>
  <done>Migration directory is created and applied locally with no destructive operations.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate Prisma client and run baseline quality checks</name>
  <files>prisma/schema.prisma</files>
  <action>Regenerate Prisma client and run project lint/build checks to confirm schema additions do not break existing compile/lint baseline. Keep this phase schema-only; if any failures appear outside schema work, report without broad refactoring.</action>
  <verify>
    <automated>npx prisma generate && npm run lint && npm run build</automated>
  </verify>
  <done>Prisma client generation, lint, and build all pass after Task/Dependency schema and migration changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate dev --name add_task_dependency_models` succeeds
- [ ] `npx prisma generate` succeeds
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] No API route files or service layer files are changed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Task and Dependency schema exists with required fields/enums/relations
- Migration generated through Prisma discipline only
- Phase remains schema-only with no business-logic implementation
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
