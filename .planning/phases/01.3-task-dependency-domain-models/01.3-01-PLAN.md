---
phase: 01.3-task-dependency-domain-models
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_task_dependency_models/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: true
requirements:
  - PH1.3-TASK-MODEL
  - PH1.3-DEPENDENCY-MODEL
  - PH1.3-EXPLICIT-ENUM-DEFINITIONS
  - PH1.3-TASK-TENANT-ID
  - PH1.3-SCHEMA-ONLY-DISCIPLINE
user_setup: []
must_haves:
  truths:
    - "Prisma schema defines Task and Dependency models with all required fields and explicitly named enums from phase scope."
    - "Task includes explicit tenantId field with a relation to Tenant.id."
    - "Task and Dependency relations enforce tenant-safe ownership paths through existing tenant-scoped entities."
    - "A Prisma migration is generated and applies without direct database edits."
    - "Existing model definitions are not modified."
    - "Existing enum values are not changed."
    - "No application route/service business logic is added or modified in this phase."
  artifacts:
    - "prisma/schema.prisma includes explicit enums TaskLevel, TaskState, TaskVisibility, ApprovalPolicy, ClockMode, DependencyType plus models Task and Dependency."
    - "prisma/migrations/*_add_task_dependency_models/migration.sql creates corresponding tables, FKs, and indexes."
  key_links:
    - "Task.tenantId references Tenant.id."
    - "Task.ecoId references ECO.id; Task.ownerRoleId references Role.id."
    - "Task.parentTaskId self-references Task.id as nullable hierarchy link."
    - "Dependency.fromTaskId and Dependency.toTaskId both reference Task.id."
---

<objective>
Add Task and Dependency Prisma domain models aligned to the Preliminary Design Spec while preserving current architecture boundaries.

Purpose: establish workflow data-model foundation without introducing business logic.
Output: updated `prisma/schema.prisma` and a Prisma migration for new tables/relations/enums only.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/01.3-task-dependency-domain-models/01.3-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit enums and Task/Dependency models to Prisma schema</name>
  <files>prisma/schema.prisma</files>
  <action>Add explicit Prisma enums with exact required values: TaskLevel(MILESTONE, STEP, SUBSTEP), TaskState(NOT_STARTED, IN_PROGRESS, BLOCKED, PENDING_APPROVAL, DONE, NOT_REQUIRED, CANCELLED), TaskVisibility(INTERNAL_ONLY, CUSTOMER_VISIBLE, CUSTOMER_ACTIONABLE), ApprovalPolicy(NONE, SINGLE, SEQUENTIAL, PARALLEL, QUORUM), ClockMode(ACTIVE, WAITING_ON_CUSTOMER, WAITING_ON_SUPPLIER, WAITING_INTERNAL), DependencyType(FINISH_TO_START, START_TO_START). Add Task model fields: tenantId (required relation to Tenant.id), ecoId, parentTaskId nullable, taskLevel, name, ownerRoleId, state, visibility, approvalPolicy, clockMode, createdAt, updatedAt. Add Dependency model fields: fromTaskId, toTaskId, type, lagMinutes. Wire relations: Task->Tenant, Task->ECO, Task->Role, Task self-parent, Dependency task-edge relations. Do not modify any existing model definitions. Do not change any existing enum values.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema parses successfully and contains the exact required enums/values and Task/Dependency fields including Task.tenantId -> Tenant relation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for Task/Dependency schema changes</name>
  <files>prisma/migrations/*_add_task_dependency_models/migration.sql, prisma/migrations/migration_lock.toml</files>
  <action>Run Prisma migration creation for the new schema (`npx prisma migrate dev --name add_task_dependency_models`). Confirm generated SQL creates Task/Dependency tables, enum usage, foreign keys, and non-destructive changes only. Explicitly verify migration scope is additive for new enums/models and does not alter pre-existing model structures or existing enum values. Do not hand-edit database directly.</action>
  <verify>
    <automated>npx prisma migrate dev --name add_task_dependency_models</automated>
  </verify>
  <done>Migration directory is created and applied locally with no destructive operations and no unintended changes to existing models/enums.</done>
</task>

<task type="auto">
  <name>Task 3: Validate immutability constraints and run baseline quality checks</name>
  <files>prisma/schema.prisma</files>
  <action>Check git diff to ensure only intended Task/Dependency enum/model additions are present and that no existing model definitions or existing enum values changed. Then regenerate Prisma client and run project lint/build checks to confirm schema additions do not break existing compile/lint baseline. Keep this phase schema-only; if any failures appear outside schema work, report without broad refactoring.</action>
  <verify>
    <automated>git diff -- prisma/schema.prisma prisma/migrations | cat && npx prisma generate && npm run lint && npm run build</automated>
  </verify>
  <done>Diff review confirms immutability constraints were honored, and Prisma client generation, lint, and build all pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate dev --name add_task_dependency_models` succeeds
- [ ] Required enums exist with exact values:
  - [ ] `TaskLevel`: `MILESTONE`, `STEP`, `SUBSTEP`
  - [ ] `TaskState`: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `PENDING_APPROVAL`, `DONE`, `NOT_REQUIRED`, `CANCELLED`
  - [ ] `TaskVisibility`: `INTERNAL_ONLY`, `CUSTOMER_VISIBLE`, `CUSTOMER_ACTIONABLE`
  - [ ] `ApprovalPolicy`: `NONE`, `SINGLE`, `SEQUENTIAL`, `PARALLEL`, `QUORUM`
  - [ ] `ClockMode`: `ACTIVE`, `WAITING_ON_CUSTOMER`, `WAITING_ON_SUPPLIER`, `WAITING_INTERNAL`
  - [ ] `DependencyType`: `FINISH_TO_START`, `START_TO_START`
- [ ] `Task.tenantId` exists and references `Tenant.id`
- [ ] `npx prisma generate` succeeds
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Existing model definitions unchanged
- [ ] Existing enum values unchanged
- [ ] No API route files or service layer files are changed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Task and Dependency schema exists with required fields/enums/relations
- Explicit enum definitions and values match required set exactly
- Task contains explicit `tenantId` relation to `Tenant`
- Migration generated through Prisma discipline only
- Existing models and existing enum values remain unchanged
- Phase remains schema-only with no business-logic implementation
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
