---
phase: 01.6-gate-domain-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_add_gate_model/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: true
requirements:
  - PH1.6-GATE-MODEL
  - PH1.6-TENANT-SCOPED-GATE
  - PH1.6-INVERSE-RELATION
  - PH1.6-SCHEMA-ONLY-DISCIPLINE
user_setup: []
must_haves:
  truths:
    - "Prisma schema defines Gate with required fields: taskId, tenantId, type, condition, createdAt."
    - "Gate type enum is explicitly limited to PRECONDITION and POSTCONDITION."
    - "Gate references Task and Tenant via required foreign keys."
    - "Gate includes @@index([tenantId])."
    - "Task includes required inverse relation to Gate."
    - "No existing model fields are modified, renamed, or removed except required inverse relation additions."
    - "A Prisma migration is generated and applied without direct DB edits."
    - "No service or route files are modified."
  artifacts:
    - "prisma/schema.prisma includes GateType enum and Gate model plus required inverse relation additions."
    - "prisma/migrations/*_add_gate_model/migration.sql creates Gate structures, constraints, and foreign keys."
  key_links:
    - "Gate.taskId references Task.id."
    - "Gate.tenantId references Tenant.id."
    - "Task has inverse relation field to Gate."
---

<objective>
Introduce a Gate domain model aligned to the Design Spec while preserving strict schema-only and tenant-isolation constraints.

Purpose: establish persistent gate metadata for task preconditions/postconditions without runtime/business-logic changes.
Output: updated `prisma/schema.prisma` and a Prisma migration for Gate structures only.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/01.6-gate-domain-model/01.6-CONTEXT.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gate schema with required fields and relations</name>
  <files>prisma/schema.prisma</files>
  <action>Add `GateType` enum with exact values `PRECONDITION` and `POSTCONDITION`. Add `Gate` model with required fields: taskId, tenantId, type, condition Json (required), createdAt. Add required relations to Task and Tenant. Add `@@index([tenantId])`. Add only required inverse relation field on Task. Do not modify existing model fields other than required inverse relation additions.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema validates and includes Gate model, enum values, required relations, tenant index, and required Task inverse relation.</done>
</task>

<task type="auto">
  <name>Task 2: Generate migration for Gate schema changes</name>
  <files>prisma/migrations/*_add_gate_model/migration.sql, prisma/migrations/migration_lock.toml</files>
  <action>Run Prisma migration creation (`npx prisma migrate dev --name add_gate_model`). Confirm generated SQL creates Gate enum/table, tenant index, and required foreign keys to Task and Tenant. Ensure migration scope is additive and does not modify existing model fields except required inverse relation support.</action>
  <verify>
    <automated>npx prisma migrate dev --name add_gate_model</automated>
  </verify>
  <done>Migration file is created/applied and SQL reflects required Gate schema and foreign-key links.</done>
</task>

<task type="auto">
  <name>Task 3: Run generation and baseline checks with scope review</name>
  <files>prisma/schema.prisma</files>
  <action>Inspect schema/migration diff to verify only additive Gate changes plus required inverse relation additions were made. Run Prisma client generation and project lint/build checks to confirm no runtime breakage from schema-only changes. Report pre-existing baseline issues without refactoring.</action>
  <verify>
    <automated>git diff -- prisma/schema.prisma prisma/migrations | cat && npx prisma generate && npm run lint && npm run build</automated>
  </verify>
  <done>Diff confirms scope constraints, Prisma client is generated, and baseline checks complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate dev --name add_gate_model` succeeds
- [ ] Gate includes `taskId`, `tenantId`, `type`, `condition`, `createdAt`
- [ ] `GateType` enum includes exactly `PRECONDITION`, `POSTCONDITION`
- [ ] Gate references `Task` and `Tenant`
- [ ] Gate includes `@@index([tenantId])`
- [ ] Task includes inverse relation to Gate
- [ ] Existing model fields unchanged except required inverse relation additions
- [ ] `npx prisma generate` succeeds
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced by Gate schema changes
- Gate model is present and migrated
- Phase remains schema-only with sequential execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
