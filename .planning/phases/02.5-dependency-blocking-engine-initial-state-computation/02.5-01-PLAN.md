---
phase: 02.5-dependency-blocking-engine-initial-state-computation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/app/api/ecos/[id]/instantiate/route.ts
autonomous: true
requirements:
  - PH2.5-INITIAL-STATE-COMPUTATION
  - PH2.5-INCOMING-DEPENDENCY-BLOCKING
  - PH2.5-FIRST-INSTANTIATION-ONLY
  - PH2.5-NO-MUTATION-EXISTING-TASKS
  - PH2.5-DETERMINISTIC-STATE-UPDATE
  - PH2.5-RESULT-COUNTS
  - PH2.5-TENANT-ISOLATION
  - PH2.5-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "After runtime dependencies are created, newly created tasks with zero incoming runtime dependencies are set to NOT_STARTED."
    - "After runtime dependencies are created, newly created tasks with one or more incoming runtime dependencies are set to BLOCKED."
    - "Incoming dependency presence is computed from runtime Dependency rows (fromTaskId/toTaskId), not blueprint definitions."
    - "Initial blocking-state logic runs only on first instantiation and is skipped in idempotent no-op path."
    - "Pre-existing tasks are not re-evaluated or mutated by this phase."
    - "State updates run deterministically after dependency creation."
    - "Structured result includes blockedTasks and readyTasks counts."
    - "No dynamic transition engine is introduced in this phase."
    - "No gates/approvals are evaluated."
    - "Service/route use tenantDb wrapper only; no raw Prisma in service/route."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave plan."
  artifacts:
    - "src/lib/db.ts exposes tenant-scoped task-state update helper(s) for created task ids."
    - "src/services/template-instantiation.service.ts computes initial task state from incoming runtime dependencies and returns blocked/ready counts."
    - "src/app/api/ecos/[id]/instantiate/route.ts returns updated structured response fields if needed."
  key_links:
    - "State computation executes after runtime dependency materialization."
    - "Incoming dependency checks use runtime Dependency records created in the instantiation path."
    - "Only created task ids are candidates for state updates."
    - "Idempotent no-op branch returns deterministic zero blocked/ready counts."
---

<objective>
Compute initial Task.state from runtime dependencies during first template instantiation.

Purpose: establish deterministic initial blocking semantics without introducing dynamic transitions or schema changes.
Output: service + tenantDb updates that assign NOT_STARTED/BLOCKED on created tasks and return blocking summary counts.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.5-dependency-blocking-engine-initial-state-computation/02.5-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/ecos/[id]/instantiate/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper for deterministic task state updates</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped helper method(s) required to update task state for a provided list of created task ids only. Ensure operations remain tenant-filtered and wrapper-contained. Keep this phase service-layer only and avoid raw Prisma usage in service/route.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb supports tenant-scoped state updates for created runtime tasks.</done>
</task>

<task type="auto">
  <name>Task 2: Compute and apply initial blocking state in instantiateTemplateForEco</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>After runtime dependency creation, compute incoming dependency presence for each created task from runtime `Dependency` rows (`fromTaskId`/`toTaskId`) created in the instantiation path, not from blueprint definitions. Set task state to `BLOCKED` for tasks with one or more incoming runtime dependencies and `NOT_STARTED` for tasks with zero incoming runtime dependencies. Apply deterministically and only for created task ids in first-instantiation path. Keep idempotent no-op path untouched and do not re-evaluate pre-existing tasks. Do not add dynamic transition logic or gate/approval evaluation. Return structured result including `blockedTasks` and `readyTasks` counts.</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>Service computes and applies initial blocking state for created tasks with deterministic counts in response.</done>
</task>

<task type="auto">
  <name>Task 3: Keep route contract aligned and verify checks</name>
  <files>src/app/api/ecos/[id]/instantiate/route.ts, src/services/template-instantiation.service.ts</files>
  <action>Update route response passthrough only as needed so `blockedTasks` and `readyTasks` are included in structured results. Keep route thin and free of Prisma usage. Run lint/build/curl verification path and confirm no schema/model changes were introduced.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/ecos/REPLACE_ECO_ID/instantiate -H 'content-type: application/json' -d '{"tenantId":"REPLACE_TENANT_ID","templateVersionId":"REPLACE_TEMPLATE_VERSION_ID"}' || true)</automated>
  </verify>
  <done>Endpoint returns blocking summary counts through service response while preserving architecture constraints.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Newly created tasks with zero incoming runtime dependencies are set to NOT_STARTED
- [ ] Newly created tasks with one or more incoming runtime dependencies are set to BLOCKED
- [ ] Incoming dependency presence is determined from runtime Dependency rows (fromTaskId/toTaskId) created during instantiation, not blueprint definitions
- [ ] State computation runs only on first instantiation path
- [ ] Idempotent no-op path is untouched
- [ ] Pre-existing tasks are not re-evaluated or mutated
- [ ] State updates happen deterministically after dependency creation
- [ ] Result includes blockedTasks and readyTasks counts
- [ ] No dynamic transition logic introduced
- [ ] No gates/approvals evaluated
- [ ] Service/route use tenantDb wrapper only; no raw Prisma in service/route
- [ ] `prisma/schema.prisma` unchanged
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Initial blocking state computation works for newly instantiated tasks
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
