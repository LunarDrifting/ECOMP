---
phase: 02.5-dependency-blocking-engine-initial-state-computation
plan: "01"
subsystem: service
tags: [service-layer, state-engine, dependencies, tenant-isolation]
requires: []
provides:
  - Initial blocking-state computation for newly created tasks
  - Runtime-dependency-based incoming-edge evaluation
  - Structured blocked/ready counts in instantiation result
affects: [workflow, instantiation, api]
tech-stack:
  added: []
  patterns: [runtime dependency driven initial state assignment]
key-files:
  created: []
  modified:
    - src/lib/db.ts
    - src/services/template-instantiation.service.ts
key-decisions:
  - "Computed initial states only from runtime Dependency rows created during current instantiation, not blueprint definitions."
  - "Applied state updates only to created task ids, preserving idempotent no-op branch and existing task immutability."
  - "Used deterministic sorted task-id batches for BLOCKED and NOT_STARTED updates."
patterns-established:
  - "Initial blocking semantics are create-time only and do not introduce dynamic transition logic."
  - "State updates remain tenant-scoped through tenantDb wrapper methods."
requirements-completed:
  - PH2.5-INITIAL-STATE-COMPUTATION
  - PH2.5-INCOMING-DEPENDENCY-BLOCKING
  - PH2.5-FIRST-INSTANTIATION-ONLY
  - PH2.5-NO-MUTATION-EXISTING-TASKS
  - PH2.5-DETERMINISTIC-STATE-UPDATE
  - PH2.5-RESULT-COUNTS
  - PH2.5-TENANT-ISOLATION
  - PH2.5-SCHEMA-STABILITY
duration: 18min
completed: 2026-02-28
---

# Phase 2.5 Plan 01 Summary

**Initial task blocking state is now computed from runtime dependency records during first instantiation and returned as blocked/ready counts.**

## Performance

- **Duration:** 18 min
- **Started:** 2026-02-28T04:02:00Z
- **Completed:** 2026-02-28T04:20:00Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments
- Added tenant-scoped task state update helper to `tenantDb` for bulk state assignment by task ids.
- Updated `instantiateTemplateForEco` to:
  - keep idempotent branch untouched and return zero counts there
  - compute incoming dependency presence from runtime dependency rows created in the current instantiation
  - set newly created tasks with incoming runtime deps to `BLOCKED`
  - set newly created tasks without incoming runtime deps to `NOT_STARTED`
  - return `blockedTasks` and `readyTasks` in structured response
- Kept route thin with no Prisma usage changes required.

## Task Commits

Each task was committed atomically where source changes existed:

1. **Task 1: Extend tenantDb wrapper for deterministic task state updates** - `e3b77b2` (feat)
2. **Task 2: Compute and apply initial blocking state in instantiateTemplateForEco** - `03464d3` (feat)
3. **Task 3: Keep route contract aligned and verify checks** - no source changes required; verification only

## Files Created/Modified
- `src/lib/db.ts` - Added `task.updateStateForIds` tenant-scoped helper.
- `src/services/template-instantiation.service.ts` - Added runtime-dependency-based blocking computation and state updates.

## Decisions Made
- Incoming dependency presence is derived strictly from runtime `Dependency` records created in this instantiation path.
- Deterministic updates are applied by sorting blocked/ready task id sets before update calls.

## Deviations from Plan

None in implementation scope.

## Issues Encountered
- `npm run lint` continues to fail on baseline pre-existing issues in unchanged files (`src/lib/db.ts` historical `any` usage and `src/app/api/admin-only/route.ts` warning).
- Curl verification could not run because no local server was reachable at `localhost:3000` in this environment.

## Verification Evidence

- `npx eslint src/lib/db.ts src/services/template-instantiation.service.ts 'src/app/api/ecos/[id]/instantiate/route.ts` (baseline errors in unchanged sections of `src/lib/db.ts`).
- `npm run lint` (same baseline failures).
- `npm run build` passed.
- `prisma/schema.prisma` unchanged.

## User Setup Required

None.

## Next Phase Readiness

- Initial blocking state is now established deterministically at instantiation time.
- Future phases can implement dynamic state transitions without changing initial state computation semantics.

---
*Phase: 02.5-dependency-blocking-engine-initial-state-computation*
*Completed: 2026-02-28*
