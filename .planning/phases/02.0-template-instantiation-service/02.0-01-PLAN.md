---
phase: 02.0-template-instantiation-service
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/app/api/ecos/[id]/instantiate/route.ts
autonomous: true
requirements:
  - PH2.0-INSTANTIATE-SERVICE
  - PH2.0-INSTANTIATE-ENDPOINT
  - PH2.0-TENANT-ISOLATION
  - PH2.0-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "Service `instantiateTemplateForEco(...)` exists and instantiates task records for an ECO from a TemplateVersion."
    - "Service creates or validates ECOPlan binding before creating tasks."
    - "All reads/writes in the flow are tenant-scoped and use tenantDb wrapper methods."
    - "Route `POST /api/ecos/:id/instantiate` delegates to service and does not directly use Prisma."
    - "Tasks are attached to ECO via `task.ecoId`; ECOPlan association is ensured through prior ECOPlan binding for the same ECO."
    - "No schema/model files are modified in this phase."
  artifacts:
    - "src/services/template-instantiation.service.ts implements instantiateTemplateForEco."
    - "src/app/api/ecos/[id]/instantiate/route.ts exposes instantiation endpoint."
    - "src/lib/db.ts includes required tenant-scoped helper methods for this flow."
  key_links:
    - "Route calls instantiateTemplateForEco and returns service result."
    - "Service uses tenantDb(tenantId) methods for ECO/TemplateVersion/ECOPlan/Task operations."
    - "Service ensures ECOPlan exists before Task create operations."
---

<objective>
Implement service-layer template instantiation for ECO workflows, with a new API endpoint that remains thin and tenant-safe.

Purpose: enable controlled creation of task instances from a selected template version without schema changes.
Output: tenant-scoped service logic, supporting db wrapper methods, and endpoint verification via curl.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/02.0-template-instantiation-service/02.0-CONTEXT.md
@src/lib/db.ts
@src/services/bootstrap.service.ts
@src/app/api/bootstrap/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb wrapper for instantiation flow</name>
  <files>src/lib/db.ts</files>
  <action>Add tenant-scoped helper methods needed by instantiation service: ECO lookup by id within tenant, TemplateVersion lookup (scoped through template tenant path), ECOPlan create/find by ecoId+tenantId, and Task create/createMany with enforced tenant context via ECO relation constraints. Keep all methods routed through tenantDb and avoid exposing raw Prisma models for new flow operations.</action>
  <verify>
    <automated>npm run lint -- src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes the required tenant-safe operations for ECOPlan-first task instantiation without raw Prisma usage in downstream route code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement instantiateTemplateForEco service</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Create `instantiateTemplateForEco({ tenantId, ecoId, templateVersionId, actorId? })` service following existing service pattern. Service must: validate tenant-scoped ECO and TemplateVersion, create or ensure ECOPlan binding first, then create Task records attached to ECO (`ecoId`) after ECOPlan exists. Enforce tenant scoping on every query via tenantDb methods only. Keep behavior deterministic and return structured result (created/reused ECOPlan, tasks created count/ids).</action>
  <verify>
    <automated>npm run lint -- src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>Service function exists, performs ECOPlan-first task instantiation, and never bypasses tenantDb.</done>
</task>

<task type="auto">
  <name>Task 3: Add instantiate API route and verify with curl</name>
  <files>src/app/api/ecos/[id]/instantiate/route.ts</files>
  <action>Add `POST /api/ecos/[id]/instantiate` App Router route that parses body (`tenantId`, `templateVersionId`, optional `actorId`), validates required inputs, and delegates to `instantiateTemplateForEco`. Do not import/use Prisma in route. Return JSON success payload and error status mapping. Verify endpoint with curl request against local dev server and capture expected status/payload shape. Confirm no schema files changed in this phase.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/ecos/REPLACE_ECO_ID/instantiate -H 'content-type: application/json' -d '{\"tenantId\":\"REPLACE_TENANT_ID\",\"templateVersionId\":\"REPLACE_TEMPLATE_VERSION_ID\"}' || true)</automated>
  </verify>
  <done>Endpoint exists and calls service correctly; curl invocation path is verified; route contains no direct Prisma usage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `instantiateTemplateForEco(...)` implemented in service layer
- [ ] `POST /api/ecos/:id/instantiate` route added
- [ ] Route has no direct Prisma import/usage
- [ ] ECOPlan is created/ensured before task creation
- [ ] Tasks are attached to ECO and implicitly to ECOPlan via ECOPlan<->ECO binding
- [ ] Tenant scoping enforced on all queries via tenantDb
- [ ] `prisma/schema.prisma` unchanged
- [ ] `npm run lint` completes (report baseline issues if pre-existing)
- [ ] `npm run build` passes
- [ ] curl verification command path documented/executed
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Service-layer instantiation flow works through endpoint
- No schema/model modifications introduced
- Phase remains sequential with single-wave execution
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
