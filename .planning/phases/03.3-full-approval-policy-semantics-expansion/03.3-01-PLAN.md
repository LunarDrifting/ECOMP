---
phase: 03.3-full-approval-policy-semantics-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/services/template-instantiation.service.ts
  - src/app/api/tasks/[id]/complete/route.ts
autonomous: true
requirements:
  - PH3.3-APPROVAL-POLICY-NONE
  - PH3.3-APPROVAL-POLICY-SINGLE
  - PH3.3-APPROVAL-POLICY-SEQUENTIAL
  - PH3.3-APPROVAL-POLICY-PARALLEL
  - PH3.3-APPROVAL-POLICY-QUORUM
  - PH3.3-DETERMINISTIC-409-ERROR
  - PH3.3-BATCHED-APPROVAL-READS
  - PH3.3-TENANT-ISOLATION
  - PH3.3-IDEMPOTENCY-PRESERVATION
  - PH3.3-SCHEMA-STABILITY
user_setup: []
must_haves:
  truths:
    - "markTaskDone enforces policy-specific approval semantics for NONE, SINGLE, SEQUENTIAL, PARALLEL, and QUORUM."
    - "Approval policy evaluation uses tenant-scoped approval data loaded once per markTaskDone call."
    - "NONE policy requires no approvals and remains unchanged."
    - "SINGLE policy requires at least one APPROVED approval row."
    - "SEQUENTIAL policy rejects when any REJECTED exists; otherwise requires at least one APPROVED."
    - "PARALLEL policy requires at least one APPROVED approval from an actor whose roleId equals task.ownerRoleId."
    - "PARALLEL policy does not implement required-role matrices, distinct-role counting, or dynamic role inference."
    - "QUORUM policy requires at least two APPROVED approvals from distinct actorIds."
    - "Policy failure uses canonical message: Approval policy requirements not satisfied."
    - "Policy failures return deterministic 409 with no state mutation and no resolver invocation."
    - "Existing BLOCKED rejection, already-DONE idempotency, and PRECONDITION gate enforcement ordering remain preserved."
    - "Projection layer remains unchanged."
    - "No schema/model files are modified in this phase."
    - "Execution remains sequential as a single-wave phase."
  artifacts:
    - "src/services/template-instantiation.service.ts includes deterministic policy evaluation block for all non-NONE policies."
    - "src/lib/db.ts exposes batched tenant-scoped approval and actor-roleId read helpers required for policy evaluation."
    - "src/app/api/tasks/[id]/complete/route.ts maps canonical approval policy failure to 409 deterministically."
  key_links:
    - "Approval policy checks run after BLOCKED/already-DONE/authz checks and before gate evaluation/state mutation."
    - "Resolver invocation remains post-DONE mutation only."
    - "No N+1 query patterns are introduced."
---

<objective>
Implement deterministic, full approval policy semantics in `markTaskDone` for `SINGLE`, `SEQUENTIAL`, `PARALLEL`, and `QUORUM`.

Purpose: enforce policy-correct completion gating while preserving existing transition flow guarantees.
Output: service-level policy evaluator, batched tenant-scoped reads, deterministic 409 behavior.
</objective>

<execution_context>
Use the standard GSD execute workflow and summary template:
- /Users/scottb/.codex/get-shit-done/workflows/execute-plan.md
- /Users/scottb/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ARCHITECTURE_CONTRACT.md
@docs/ROADMAP.md
@.planning/phases/03.3-full-approval-policy-semantics-expansion/03.3-CONTEXT.md
@src/lib/db.ts
@src/services/template-instantiation.service.ts
@src/app/api/tasks/[id]/complete/route.ts
@src/services/workflow-projection.service.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend tenantDb with batched approval-role read helpers for policy evaluation</name>
  <files>src/lib/db.ts</files>
  <action>Add/adjust tenant-scoped read helpers so markTaskDone can evaluate policies with one approval fetch and one actor-roleId fetch. Ensure helper outputs support approved/rejected checks, PARALLEL owner-role matching, and QUORUM distinct-actor counting without N+1 queries.</action>
  <verify>
    <automated>npx eslint src/lib/db.ts || npm run lint</automated>
  </verify>
  <done>tenantDb exposes batched tenant-scoped data needed for deterministic policy evaluation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic full approval policy evaluator in markTaskDone</name>
  <files>src/services/template-instantiation.service.ts</files>
  <action>Replace minimal non-NONE approval check with policy-specific evaluation for SINGLE/SEQUENTIAL/PARALLEL/QUORUM using batched tenant-scoped data. Implement PARALLEL minimally: require at least one APPROVED approval whose actor has roleId == task.ownerRoleId; no role-matrix expansion. Implement QUORUM as >=2 APPROVED rows from distinct actorIds. Use canonical failure message `Approval policy requirements not satisfied` on policy miss. Preserve existing ordering guarantees: BLOCKED and already-DONE checks remain intact, authz enforcement remains intact, policy check runs before gate check and mutation, resolver remains post-DONE mutation only.</action>
  <verify>
    <automated>npx eslint src/services/template-instantiation.service.ts || npm run lint</automated>
  </verify>
  <done>markTaskDone applies deterministic policy semantics without mutating on failure.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure deterministic 409 mapping for canonical approval policy failure</name>
  <files>src/app/api/tasks/[id]/complete/route.ts</files>
  <action>Keep route thin and Prisma-free. Ensure canonical approval policy failure message maps to HTTP 409 deterministically without leaking evaluation internals. Do not change route structure beyond required error mapping behavior.</action>
  <verify>
    <automated>npm run lint && npm run build && (curl -s -X POST http://localhost:3000/api/tasks/REPLACE_TASK_ID/complete -H 'content-type: application/json' -d '{"tenantId":"REPLACE_TENANT_ID","taskId":"REPLACE_TASK_ID","actorId":"REPLACE_USER_ID"}' || true)</automated>
  </verify>
  <done>Completion endpoint returns deterministic 409 on policy failure with unchanged thin-route pattern.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] NONE policy allows completion without approval requirement (subject to existing checks)
- [ ] SINGLE policy allows with >=1 APPROVED and rejects with 0 APPROVED
- [ ] SEQUENTIAL policy rejects when any REJECTED exists
- [ ] SEQUENTIAL policy allows when >=1 APPROVED and no REJECTED
- [ ] PARALLEL policy allows when at least one APPROVED approval exists from actor roleId == task.ownerRoleId
- [ ] PARALLEL policy rejects when no APPROVED approval matches task.ownerRoleId
- [ ] PARALLEL policy does not enforce required-role matrices or distinct-role counting
- [ ] QUORUM policy allows with >=2 APPROVED approvals from distinct actorIds
- [ ] QUORUM policy rejects with <2 APPROVED distinct actorIds
- [ ] Canonical failure message is exactly: Approval policy requirements not satisfied
- [ ] Canonical policy failure maps to deterministic 409
- [ ] No mutation occurs on policy failure
- [ ] Resolver is not called on policy failure
- [ ] Unauthorized actors remain blocked per phase 03.2 authz logic
- [ ] BLOCKED tasks still reject before approval policy evaluation
- [ ] PRECONDITION gate enforcement still executes after policy success
- [ ] Already-DONE remains idempotent no-op
- [ ] Policy evaluation uses one batched approval load and one batched actor-roleId load (no N+1)
- [ ] Projection layer unchanged
- [ ] No schema/model changes
- [ ] `npm run build` passes
</verification>

<success_criteria>
- All tasks completed
- Deterministic policy-specific approval semantics fully enforced in markTaskDone
- Canonical 409 policy failure behavior present without internal detail leakage
- Existing write-side flow guarantees preserved outside expanded policy evaluation
- Phase remains sequential as a single-wave plan
</success_criteria>

<output>
After completion, create the phase summary file for plan `01` in this phase directory.
</output>
